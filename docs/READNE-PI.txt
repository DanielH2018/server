Commands to run to setup Pi:
sudo apt-get update && sudo apt-get upgrade
sudo apt-get reinstall network-manager

# run this first
$ sudo apt-get install 
linux-modules-extra-raspi
# run this then
$ sudo apt-get install openvswitch-switch-dpdk
