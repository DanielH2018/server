---
generated_from: scripts/docs/reference/decisions.py
generated_at: 2026-09-24 19:27 UTC
generated_sha: 951a11dba
---

!!! warning "Generated file — do not edit"
    This page is rendered from the Ansible tree by `scripts/docs/reference/decisions.py`. Hand edits are
    overwritten by the next run, and a prek hook rejects them at commit time.
    To change what appears here, change the generator or the source it reads.


# Decisions

436 `DECIDED:` marker(s) found across the tree. A marker is a settled trade-off recorded as a comment at the line it governs, per CLAUDE.md's "Review & Memory Hygiene" section — written so a reviewer trips over the reasoning before re-opening a decision that already has one. A reviewer brief greps the literal marker text before flagging something as new; this page exists so a human can browse the same set instead of guessing a phrase to grep.

!!! warning "Possible duplicates"
    Two markers below have a near-identical first sentence once case and whitespace are normalised — usually the same trade-off decided twice, or a marker copied and never specialised. Worth a look, not a verdict.

    * `.claude/hooks/_claude_guard.py:19` and `scripts/dev/_claude_worktree.py:10`
    * `.claude/hooks/block-footguns.sh:9` and `.claude/hooks/block-protected-bash.sh:10`
    * `.claude/hooks/block-footguns.sh:9` and `.claude/hooks/block-protected-edits.sh:12`
    * `.claude/hooks/block-footguns.sh:9` and `.claude/hooks/nudge-land-sh.sh:8`
    * `.claude/hooks/block-protected-bash.sh:10` and `.claude/hooks/block-protected-edits.sh:12`
    * `.claude/hooks/block-protected-bash.sh:10` and `.claude/hooks/nudge-land-sh.sh:8`
    * `.claude/hooks/block-protected-edits.sh:12` and `.claude/hooks/nudge-land-sh.sh:8`
    * `.claude/rules/facts.md:30` and `scripts/lib/tests/test_facts_citations.py:79`
    * `ansible/roles/containers/autoheal/CLAUDE.md:47` and `ansible/tests/repo/test_adr_links.py:13`
    * `ansible/roles/containers/autoheal/CLAUDE.md:47` and `docs/archive/docs-ui-and-adrs/design.md:27`
    * `ansible/roles/k8s/claude-otel/tasks/main.yml:278` and `ansible/roles/k8s/configarr/tasks/main.yml:97`
    * `ansible/roles/k8s/claude-otel/templates/collector.yaml.j2:94` and `ansible/roles/k8s/claude-otel/templates/grafana.yaml.j2:129`
    * `ansible/roles/k8s/claude-otel/templates/collector.yaml.j2:94` and `ansible/roles/k8s/claude-otel/templates/kube-state-metrics.yaml.j2:86`
    * `ansible/roles/k8s/claude-otel/templates/collector.yaml.j2:94` and `ansible/roles/k8s/claude-otel/templates/loki.yaml.j2:86`
    * `ansible/roles/k8s/claude-otel/templates/collector.yaml.j2:94` and `ansible/roles/k8s/claude-otel/templates/prometheus.yaml.j2:850`
    * `ansible/roles/k8s/claude-otel/templates/collector.yaml.j2:94` and `ansible/roles/k8s/claude-otel/templates/tempo.yaml.j2:80`
    * `ansible/roles/k8s/claude-otel/templates/grafana.yaml.j2:129` and `ansible/roles/k8s/claude-otel/templates/kube-state-metrics.yaml.j2:86`
    * `ansible/roles/k8s/claude-otel/templates/grafana.yaml.j2:129` and `ansible/roles/k8s/claude-otel/templates/loki.yaml.j2:86`
    * `ansible/roles/k8s/claude-otel/templates/grafana.yaml.j2:129` and `ansible/roles/k8s/claude-otel/templates/prometheus.yaml.j2:850`
    * `ansible/roles/k8s/claude-otel/templates/grafana.yaml.j2:129` and `ansible/roles/k8s/claude-otel/templates/tempo.yaml.j2:80`
    * `ansible/roles/k8s/claude-otel/templates/kube-state-metrics.yaml.j2:86` and `ansible/roles/k8s/claude-otel/templates/loki.yaml.j2:86`
    * `ansible/roles/k8s/claude-otel/templates/kube-state-metrics.yaml.j2:86` and `ansible/roles/k8s/claude-otel/templates/prometheus.yaml.j2:850`
    * `ansible/roles/k8s/claude-otel/templates/kube-state-metrics.yaml.j2:86` and `ansible/roles/k8s/claude-otel/templates/tempo.yaml.j2:80`
    * `ansible/roles/k8s/claude-otel/templates/loki.yaml.j2:86` and `ansible/roles/k8s/claude-otel/templates/prometheus.yaml.j2:850`
    * `ansible/roles/k8s/claude-otel/templates/loki.yaml.j2:86` and `ansible/roles/k8s/claude-otel/templates/tempo.yaml.j2:80`
    * `ansible/roles/k8s/claude-otel/templates/prometheus.yaml.j2:850` and `ansible/roles/k8s/claude-otel/templates/tempo.yaml.j2:80`
    * `ansible/roles/k8s/homelab-mcp/files/safe_reads.py:264` and `scripts/diagnostics/probe_lib/ha.py:283`
    * `ansible/roles/k8s/monitor-bridge/CLAUDE.md:199` and `ansible/roles/setup/gitops_deploy/CLAUDE.md:411`
    * `ansible/roles/k8s/monitor-bridge/CLAUDE.md:199` and `ansible/roles/setup/gitops_deploy/files/deploy_broad_k8s.py:232`
    * `ansible/roles/k8s/monitor-bridge/CLAUDE.md:199` and `scripts/validate/tests/test_validate_shell_templates.py:87`
    * `ansible/roles/k8s/monitor-bridge/templates/env-secret.yaml.j2:261` and `ansible/roles/k8s/monitor-bridge/tests/test_host_temp_rated.py:187`
    * `ansible/roles/k8s/monitor-bridge/tests/test_host_temp.py:147` and `ansible/roles/k8s/monitor-bridge/tests/test_host_temp_rated.py:6`
    * `ansible/roles/k8s/mosquitto/templates/deployment.yaml.j2:26` and `ansible/roles/k8s/pihole/templates/deployment.yaml.j2:34`
    * `ansible/roles/k8s/mosquitto/templates/deployment.yaml.j2:26` and `ansible/roles/k8s/terraria/templates/deployment.yaml.j2:27`
    * `ansible/roles/k8s/mosquitto/templates/deployment.yaml.j2:26` and `ansible/roles/k8s/traefik/templates/deployment.yaml.j2:27`
    * `ansible/roles/k8s/pi-peer-backup/templates/Dockerfile.j2:13` and `ansible/roles/k8s/valheim/templates/Dockerfile.j2:13`
    * `ansible/roles/k8s/pihole/templates/deployment.yaml.j2:34` and `ansible/roles/k8s/terraria/templates/deployment.yaml.j2:27`
    * `ansible/roles/k8s/pihole/templates/deployment.yaml.j2:34` and `ansible/roles/k8s/traefik/templates/deployment.yaml.j2:27`
    * `ansible/roles/k8s/terraria/templates/deployment.yaml.j2:27` and `ansible/roles/k8s/traefik/templates/deployment.yaml.j2:27`
    * `ansible/roles/k8s/wg-easy/CLAUDE.md:74` and `docs/gitops-pipeline.md:1541`
    * `ansible/roles/k8s/wg-easy/CLAUDE.md:74` and `docs/monitor-bridge-checks.md:445`
    * `ansible/roles/setup/gitops_deploy/CLAUDE.md:162` and `ansible/roles/setup/gitops_deploy/CLAUDE.md:255`
    * `ansible/roles/setup/gitops_deploy/CLAUDE.md:162` and `ansible/roles/setup/k3s/templates/release-staleness-check.sh.j2:9`
    * `ansible/roles/setup/gitops_deploy/CLAUDE.md:255` and `ansible/roles/setup/k3s/templates/release-staleness-check.sh.j2:9`
    * `ansible/roles/setup/gitops_deploy/CLAUDE.md:411` and `ansible/roles/setup/gitops_deploy/files/deploy_broad_k8s.py:232`
    * `ansible/roles/setup/gitops_deploy/CLAUDE.md:411` and `scripts/validate/tests/test_validate_shell_templates.py:87`
    * `ansible/roles/setup/gitops_deploy/files/deploy_broad_k8s.py:232` and `scripts/validate/tests/test_validate_shell_templates.py:87`
    * `ansible/roles/setup/initial_setup/tasks/crons.yml:646` and `ansible/roles/setup/initial_setup/tasks/crons.yml:718`
    * `ansible/tests/_ratchet.py:77` and `ansible/tests/_ratchet.py:383`
    * `ansible/tests/repo/test_adr_links.py:1` and `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:170`
    * `ansible/tests/repo/test_adr_links.py:4` and `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:173`
    * `ansible/tests/repo/test_adr_links.py:13` and `docs/archive/docs-ui-and-adrs/design.md:27`
    * `docs/adr/index.md:9` and `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:100`
    * `docs/adr/template.md:51` and `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:372`
    * `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:162` and `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:344`
    * `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:329` and `docs/archive/docs-ui-and-adrs/design.md:7`
    * `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:329` and `docs/archive/docs-ui-and-adrs/design.md:137`
    * `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:329` and `docs/gitops-pipeline.md:449`
    * `docs/archive/docs-ui-and-adrs/design.md:7` and `docs/archive/docs-ui-and-adrs/design.md:137`
    * `docs/archive/docs-ui-and-adrs/design.md:7` and `docs/post-merge-automation.md:268`
    * `docs/archive/docs-ui-and-adrs/design.md:7` and `scripts/diagnostics/probe_lib/health_kubectl.py:12`
    * `docs/archive/docs-ui-and-adrs/design.md:7` and `scripts/diagnostics/tests/test_probe_health.py:324`
    * `docs/archive/docs-ui-and-adrs/design.md:137` and `docs/post-merge-automation.md:268`
    * `docs/archive/docs-ui-and-adrs/design.md:137` and `scripts/diagnostics/probe_lib/health_kubectl.py:12`
    * `docs/archive/docs-ui-and-adrs/design.md:137` and `scripts/diagnostics/tests/test_probe_health.py:324`
    * `docs/gitops-pipeline.md:449` and `docs/post-merge-automation.md:268`
    * `docs/gitops-pipeline.md:449` and `scripts/diagnostics/probe_lib/health_kubectl.py:12`
    * `docs/gitops-pipeline.md:449` and `scripts/diagnostics/tests/test_probe_health.py:324`
    * `docs/gitops-pipeline.md:1541` and `docs/monitor-bridge-checks.md:445`
    * `docs/post-merge-automation.md:268` and `scripts/diagnostics/probe_lib/health_kubectl.py:12`
    * `docs/post-merge-automation.md:268` and `scripts/diagnostics/tests/test_probe_health.py:324`
    * `scripts/diagnostics/probe_lib/health_kubectl.py:12` and `scripts/diagnostics/tests/test_probe_health.py:324`
    * `scripts/lib/tests/test_facts_atoms.py:203` and `scripts/lib/tests/test_facts_atoms.py:210`

## roles/k8s

| Marker | File | Decided |
|---|---|---|
| ` marker and the argument; widening is an edit to that one list. | `ansible/roles/k8s/arr-notification/CLAUDE.md:40` | 2026-09-06 |
| health-only, matching what both apps already carried as UI state. for grab + import + application-update as well. That is a behaviour change, not a declaration: every episode and every movie import would post to the channel. Adopting the live set first makes the adopting PR a no-op on behaviour, and widening it later is an edit to this one list. The asymmetry is the argument — a too-narrow default costs a var edit, a too-wide one spams the channel until someone notices. | `ansible/roles/k8s/arr-notification/defaults/main.yml:20` | 2026-09-06 |
| the check is `os.path.realpath` then `str.startswith`, not `Path.resolve` then | `ansible/roles/k8s/artifacts/files/artifact_server.py:186` | 2026-09-04 |
| ` marker sits at the line; `ansible/tests/services/test_smtp_wiring.py` holds the | `ansible/roles/k8s/authelia/CLAUDE.md:199` | 2026-09-09 |
| ` marker on the rsync tolerance was amended, not reversed.** Its reasoning | `ansible/roles/k8s/authelia/CLAUDE.md:304` | 2026-09-05 |
| separate from traefik_k8s_manage_crowdsec rather than one shared variable. shared one would live in group_vars/all.yml, an `ansible/inventory/` path the GitOps deployer treats as broad — it defers and returns WITHOUT fast-forwarding, so the change would need clearing by hand. The surfaces are asymmetric too: this pod carries only an agent and PRODUCES signals, where Traefik carries a bouncer plugin and enforces bans. | `ansible/roles/k8s/authelia/defaults/main.yml:19` | 2026-08-28 |
| a second user rather than putting the OPERATOR's TOTP secret in SOPS. two_factor tier unattended means the second factor becomes another value under the age key either way, so the factor count is not what this buys. What it buys is a blast radius with an edge: revoking Claude's reach into code-server, n8n and longhorn is deleting one block from `users_database.yml`, and rotating either credential is a `sops set` plus a deploy rather than re-enrolling a phone. The operator's own login is untouched by both. | `ansible/roles/k8s/authelia/defaults/main.yml:133` | 2026-09-06 |
| `methods` is the second gate this bypass lacked. no auth of its own, so an unauthenticated `^/api/.*$` handed the LAN and every pod `DELETE /api/device/:uuid`, `POST /api/device/:uuid/smart` (forge SMART history), `POST /api/device/:uuid/archive` and `POST /api/settings` (silence notifications). Contrast the kuma-push bypass above, where the push token gates the path. Every request this rule no longer matches — any mutating verb, and any read outside the three paths below — falls through to the `*.local` one_factor rule, which carries no `resources` of its own and has no `deny` rule ahead of it. So a machine caller gets a 302 to the portal, and the browser UI is unaffected: its own `GET /api/settings` passes on the session cookie as it always did. | `ansible/roles/k8s/authelia/templates/config-secret.yaml.j2:148` | 2026-09-05 |
| the SMTP startup check stays OFF. refuses to start when the probe fails, and this portal is the forward-auth gate in front of most public routes — a Gmail blip would take SSO down for the whole fleet, including the tools needed to fix it, to protect a password-reset mail nobody is waiting on. The credential is not unwatched: monitor-bridge's `email_backstop` authenticates this exact app password on a throttle and pages through Uptime Kuma when it is revoked, so the failure this check would catch is already covered by a path that cannot take the portal down. `startup_check_address` is omitted with it. | `ansible/roles/k8s/authelia/templates/config-secret.yaml.j2:425` | 2026-09-09 |
| tolerate exit 23 only, never `\|\| true`. sidecar restart to a failed init container, and authelia rolls under Recreate — the old pod is already gone, so a non-23 rsync failure takes SSO down rather than costing one restart. Exit 23 alone is safe because the files it skips are the LAPI and online credentials, which the agent regenerates (`cscli lapi register`) on start regardless; any OTHER rsync status is a real seed failure and must fail the pod rather than start an agent with a half-populated config. | `ansible/roles/k8s/authelia/templates/deployment.yaml.j2:141` | 2026-09-05 |
| forwardAuth keeps `trustForwardHeader: true`, and the request-target headers are cleared ahead of it instead. Traefik fills X-Forwarded-Host/-Uri/-Method on the auth request only when the incoming request lacks them, and Authelia authorizes the object those three headers name. An empty value in `customRequestHeaders` deletes the header (Traefik pkg/middlewares/headers), so forwardAuth always rebuilds them from the request itself. X-Forwarded-For and X-Forwarded-Proto are deliberately NOT cleared: - X-Forwarded-For carries the client address past Cloudflare, which Authelia's regulation and network rules key on. An empty value would also leave the key present and break forwardAuth's append to it. - X-Forwarded-Proto is set by `default-headers` on the https entrypoint before any router middleware runs; clearing it here would make Authelia see `http`. Turning trustForwardHeader off instead would collapse every public client to its Cloudflare edge address for Authelia. The backend also stops receiving X-Forwarded-Host; apps fall back to the Host header, which carries the same value. | `ansible/roles/k8s/authelia/templates/forwardauth-middleware.yaml.j2:10` | 2026-09-24 |
| claude-otel tiers — Prometheus is `homelab-critical`; the other five pod templates | `ansible/roles/k8s/claude-otel/CLAUDE.md:42` | 2026-09-17 |
| this list has no folder-level prune, and that is deliberate (operator 2026-08-28). Dropping a name here removes the folder's volume and volumeMount from grafana.yaml.j2 (both rendered by `{% for folder in claude_otel_dashboard_folders %}`, at the volumeMounts and volumes blocks) but leaves the staged dir, the annotated dir and the live grafana-dashboards-<folder> ConfigMap behind. That orphan is INERT: the mount is by explicit per-folder volume name, so an unlisted ConfigMap is never mounted and never reaches Grafana. Adding a prune would trade an inert orphan for deletion behaviour that can remove a live ConfigMap — a worse failure than the one it closes, and the reason the 2026-08-28 review's fix-skeptic pass rejected it. Reopen only with a case where the orphan is reachable, not merely present. | `ansible/roles/k8s/claude-otel/defaults/main.yml:97` | 2026-08-28 |
| 0700 owned by sys_user, not 0755 — embeds a live Kuma push token. sys_user because the cron below runs as sys_user; root:root 0700 would lock out its own caller. See the same marker in roles/k8s/janitorr/tasks/main.yml. | `ansible/roles/k8s/claude-otel/tasks/main.yml:278` | 2026-08-24 |
| claude-otel tiers` in this role's CLAUDE.md. | `ansible/roles/k8s/claude-otel/templates/collector.yaml.j2:94` | 2026-09-17 |
| claude-otel tiers` in this role's CLAUDE.md. | `ansible/roles/k8s/claude-otel/templates/grafana.yaml.j2:129` | 2026-09-17 |
| claude-otel tiers` in this role's CLAUDE.md. | `ansible/roles/k8s/claude-otel/templates/kube-state-metrics.yaml.j2:86` | 2026-09-17 |
| claude-otel tiers` in this role's CLAUDE.md. | `ansible/roles/k8s/claude-otel/templates/loki.yaml.j2:86` | 2026-09-17 |
| no `origin` label here. check_targets counts against TARGETS_MIN=1, and that floor assumes exactly one series: the node job. Labelling the exportarr pods too put three more series behind the same floor, so node-exporter could die and the check would still see >= 1 and read clean. See check.py:385-397, which spells the assumption out. daniel-pi, the one host outside the cluster. STATIC targets, not discovery: the two jobs above use `role: pod`, which by construction only ever finds pods — so the Pi, which runs no kubelet, could never appear in them however many exporters it grew. That is why it contributed zero time series until these two landed. | `ansible/roles/k8s/claude-otel/templates/prometheus.yaml.j2:358` | 2026-08-25 |
| claude-otel tiers` in this role's CLAUDE.md. | `ansible/roles/k8s/claude-otel/templates/prometheus.yaml.j2:850` | 2026-09-17 |
| claude-otel tiers` in this role's CLAUDE.md. The compose bundle needs a `tempo-init` sidecar that runs `chown -R 10001:10001 /var/tempo` before Tempo starts. k8s does that natively: the kubelet applies fsGroup ownership to the volume at mount time, so the init container has no equivalent here. | `ansible/roles/k8s/claude-otel/templates/tempo.yaml.j2:80` | 2026-09-17 |
| build-time apt resolution stays, on those grounds — operator decision on #2211 (2026-09-22), which holds the snapshot-mirror evidence. | `ansible/roles/k8s/code-server/templates/Dockerfile.j2:26` | 2026-09-22 |
| 0700 owned by sys_user, not 0755 — embeds a live Kuma push token. sys_user because the cron below runs as sys_user; root:root 0700 would lock out its own caller. See the same marker in roles/k8s/janitorr/tasks/main.yml. | `ansible/roles/k8s/configarr/tasks/main.yml:97` | 2026-08-24 |
| `status=down $1`, not a bare "$1". message and was invisible to that reconstruction until 2026-08-24; both now carry the prefix. | `ansible/roles/k8s/crowdsec/templates/crowdsec-appsec-verify.sh.j2:47` | 2026-08-24 |
| `--retry 6 --retry-all-errors --retry-max-time 60` on the Kuma push curl, not the bare `--retry 3` the sibling push crons use and not the shared library's single 30s backoff. The two ipify lookups below keep `--retry 3 --retry-all-errors`; the sizing argument later in this block counts them. The push curl is one transient failure away from a red monitor: the monitor is push-type with max_retries 0, so a single bad run IS an immediate DOWN. Measured over the 3 days to 2026-08-24 the monitor made 18 transitions (9 down/up cycles) with the allowlist itself never once out of sync. | `ansible/roles/k8s/crowdsec/templates/crowdsec-update-home-allowlist.sh.j2:28` | 2026-09-17 |
| exec-curl to 127.0.0.1, not httpGet — HA's ip_ban middleware runs on EVERY request, and it keys on the peer address. A kubelet httpGet arrives from the node's pod-network gateway (10.42.0.1), so once anything gets that IP banned HA answers 403 and kubelet kills the container it just banned — a self-inflicted crash loop (2026-08-23: 3 restarts, paging k3s Workload Health). From inside the container the peer is 127.0.0.1, which no ban can reach. Readiness moves too: fixing liveness alone just relocates the outage to a yanked Service endpoint. curl over tcpSocket: tcpSocket also escapes the ban but stops proving HTTP works, so a wedged HA still holding the listener would pass. curl is proven present in this image — it was the Compose healthcheck (`curl --fail -s http://localhost:8123/`) pre-migration. timeoutSeconds: exec probes default to 1s; curl -m 4 keeps curl's deadline inside it. | `ansible/roles/k8s/home-assistant/templates/deployment.yaml.j2:119` | 2026-08-23 |
| state the floor instead of leaving it to the platform default. py/insecure-protocol reads a bare default context as permitting TLSv1 and TLSv1_1, and a hand-dismissal of that alert does not survive the next file move. (ADR-0016) | `ansible/roles/k8s/homelab-mcp/files/safe_reads.py:264` | 2026-09-02 |
| ` marker in `roles/k8s/traefik/templates/dashboard-ingressroute.yaml.j2` mean | `ansible/roles/k8s/homepage/CLAUDE.md:205` | 2026-09-10 |
| the second counter is named "Down" but matches Pending\|Failed\|Unknown, and a Pending pod is starting rather than down. The mismatch is deliberate and chosen for the dashboard, where one word fits the block and "Not Running" wrapped to two lines. Do not "correct" the name to match the phases; the two honest alternatives are to keep a two-word label, or to narrow the match to Failed\|Unknown and lose sight of a pod stuck Pending — which is the failure this counter is most likely to catch. The visible consequence: every rollout, including homepage's own, briefly shows a non-zero DOWN while new pods schedule. | `ansible/roles/k8s/homepage/defaults/main.yml:45` | 2026-09-10 |
| `display: block` (homepage's default), not `dynamic-list` — every other widget on this dashboard renders as blocks, and a list tile reads as a different kind of thing. The cost is that block mode pairs a value to a label by ARRAY POSITION, so the indices below depend on the `or` operand order in homepage_k8s_headlamp_cluster_query. Accepted because a reordering shows up as "97 NODES" — wrong at a glance rather than plausibly wrong. Keep the query's operand order and this list in the same order; changing one alone is the only way to break this quietly. ENFORCED by ansible/tests/services/test_headlamp_widget_mapping_order.py, which fails on a reordered, renamed, missing or duplicated counter. | `ansible/roles/k8s/homepage/templates/config/services.yaml.j2:51` | 2026-09-10 |
| no `when: image_builder_building` here, unlike the four tasks above. on a SKIPPED build is what lets post_tasks/k8s_image_drift_gate.yml see a stale pod sitting in front of an unchanged registry tag — the case that hid nut's week-old image for 90 minutes on 2026-08-27. Gating this for consistency with its siblings is the obvious tidy-up and it blinds the gate. ENFORCED by ansible/tests/deploy/test_built_image_drift_gate.py. | `ansible/roles/k8s/image-builder/tasks/main.yml:392` | 2026-08-27 |
| hangs off "Read the post-build digest", which is deliberately NOT gated on image_builder_building — unlike the build, the result read and the failure paths above. That is what lets a skipped build still contribute a digest here. Folding that read under image_builder_building as an optimisation would blind the gate on exactly case (2); ansible/tests/deploy/test_built_image_drift_gate.py fails if that guard reappears. | `ansible/roles/k8s/image-builder/tasks/main.yml:441` | 2026-08-27 |
| 0700 owned by sys_user, not 0755 — the script interpolates a live Kuma push token into its URL, and at 0755 any local account could read it and forge or suppress this monitor's heartbeat. Owner is sys_user, NOT root: the cron below runs as sys_user, so root:root 0700 would make it unreadable to its own caller. That is why this diverges from registry-gc.sh (0700 root:root) and the crowdsec scripts (0750 root:root) — both of those are root-run crons. Match the mode to the precedent, but the owner to the cron user. | `ansible/roles/k8s/janitorr/tasks/main.yml:72` | 2026-08-24 |
| keep this, do not "tidy it away" as dead code. of this container installed to /config/plugins, which Jellyfin never scans, and left ~122 KB there on every config volume it ran against. Nothing else reaches that path — the read-only ServiceAccount cannot exec into the pod — so this container is the only thing that can clear what it created. Inert after one run. | `ansible/roles/k8s/jellyfin/templates/deployment.yaml.j2:179` | 2026-08-25 |
| an emptyDir alone, no env var. fontconfig's cache. This is the FIRST entry in the cachedir list the image's fonts.conf ships, so with a read-only root and no mount here fontconfig finds nothing writable — it logged `Fontconfig error: No writable cache directories` once per chrome process and rescanned the whole font tree on each start. An emptyDir is the whole fix: fontconfig needs the directory to be writable, not preserved, and one rescan per pod is what it costs. No env var goes with it — the two fallbacks in that log (`$HOME/.cache/fontconfig`, `$HOME/.fontconfig`, with HOME=/nonexistent for uid 65534) are only consulted when this path fails, so XDG_CACHE_HOME would be redundant, and FONTCONFIG_PATH names the CONFIG dir rather than the cache and would change font discovery. | `ansible/roles/k8s/karakeep/templates/deployment-chrome.yaml.j2:91` | 2026-09-05 |
| no readOnlyRootFilesystem here, though a steady-state meilisearch writes only under /meili_data. MEILI_UPGRADE_DB below runs the in-place index migration on exactly one start — the first after an image bump — and on no other. A read-only root would therefore pass the deploy that introduces it and could still break a future Renovate bump, whose recovery is a full "Reindex All Bookmarks". A change whose riskiest path is structurally unexercised by the deploy that validates it is the shape that lands green and is inert; the migration is the reason to decline it here, not an absence of evidence about the steady state. | `ansible/roles/k8s/karakeep/templates/deployment-meilisearch.yaml.j2:36` | 2026-09-05 |
| no readOnlyRootFilesystem here, unlike the wait-for-karakeep init container above. The `uv pip install ... --system` in the command block installs five packages into the interpreter's site-packages, on the image's own root filesystem, on EVERY container start — `--system` is what targets it there rather than a venv. A read-only root makes that first command fail, so the container never reaches the script. This is readable off the template; it needed no live measurement. Redirecting the install into a mounted emptyDir instead (a venv on /work, or PYTHONUSERBASE) would make read-only reachable, but it re-resolves the same install against a cache that no longer persists and changes how the container boots — a bigger change than the hardening it buys, and out of scope for #1129. | `ansible/roles/k8s/karakeep/templates/deployment-time-tagger.yaml.j2:88` | 2026-09-05 |
| no readOnlyRootFilesystem here, unlike the chrome sidecar and the wait-for-deps init container above. Three facts, and the third is why this is recorded rather than tried: 1. PID 1 is s6-overlay's `/init` (upstream `docker/Dockerfile`'s ENTRYPOINT, and the process was measured as s6-svscan at uid 0 — see the ("karakeep", "karakeep") entry in _ENTRYPOINT_DROPS, ansible/tests/k8s/test_container_security_context.py). s6 rebuilds its whole service tree under /run on every start, so a read-only root needs an emptyDir at /run plus S6_READ_ONLY_ROOT=1, which changes how the init itself boots. 2. The image hardcodes HOME=/root, so anything cache-writing lands on the root filesystem rather than a mounted path. That is its own open issue; it is not fixed here. 3. Guessing wrong is a manual recovery, not a rollback: k8s_autodeploy is false, strategy is Recreate, and the PVC is ReadWriteOnce, so the old pod is gone before the replacement's mounts are known-good. Measuring the writable-path set would settle 1 and 2, and it needs a deploy — the read-only ServiceAccount is Forbidden on pods/exec, so no live census is possible from a review session. | `ansible/roles/k8s/karakeep/templates/deployment.yaml.j2:66` | 2026-09-05 |
| default false, armed per-role rather than globally. (1) there is no shared metadata-labels macro yet, so every one of the other ~63 roles would need its templates hand-edited to carry `homelab/role: <service>` before --prune could ever see their objects — doing that without a proven mechanism first is the wrong order. (2) a label added to a role's manifests only protects objects APPLIED AFTER the label lands: an orphan whose staged file was already deleted before this feature shipped (the claude-otel-ingest IngressRoute this issue names) never receives the label and stays invisible to `-l homelab/role=...` — it still needs one manual kubectl delete, same as today. roles/k8s/registry is the one role armed so far, proving the mechanism end-to-end on a role with a normal Deployment+Service+NetworkPolicy shape. Arming another role means: label its object metadata the same way, list every kind it renders in manifests_prune_kinds (the default --prune-allowlist misses NetworkPolicy and every Traefik CRD), and never include Secret or PersistentVolumeClaim — see the guard test on that. | `ansible/roles/k8s/manifests/defaults/main.yml:16` | 2026-09-04 |
| one step of history, not an append-only log. an incident is "what was live before this deploy", and one step answers it. An unbounded JSONL grows by one line per service per deploy — up to 54 lines every 30-minute tick — and would need rotation, a rotation cron, and a monitor for the rotation cron. Depth beyond one step is what git history is for: every record names a commit, so the trail continues there. | `ansible/roles/k8s/manifests/tasks/release_stamp.yml:175` | 2026-08-29 |
| ` in `verdicts/host.py:hwmon_temp_limits`), and `HWMON_TEMP_RATED_MAX_C` | `ansible/roles/k8s/monitor-bridge/CLAUDE.md:156` | 2026-09-21 |
| 12 cycles` marker in `files/bridge/config_host.py`; `HWMON_TEMP_RATIO` is | `ansible/roles/k8s/monitor-bridge/CLAUDE.md:159` | 2026-09-21 |
| ` in | `ansible/roles/k8s/monitor-bridge/CLAUDE.md:199` | 2026-09-21 |
| 30m, and the floor is the worst inter-restart SPACING, not the CrashLoopBackOff 5-min backoff cap. The 2026-08-13 homepage incident above spread 31 restarts over a night, ~15-19 min apart; a window inside that spacing goes UP in the gaps and flaps — and `k3s Workload Health` is `max_retries: 0` (uptime-kuma static-monitors.yaml.j2:293), so every flap is an immediate DOWN plus a notification. That is the crowdsec-appsec failure recorded at static-monitors.yaml.j2:283-289 (24 transitions in 3h). 30m clears two spacings and six bridge cycles. The spacing could not be re-measured — cluster Prometheus retains 7d and the incident is older — so this is the conservative floor. | `ansible/roles/k8s/monitor-bridge/files/bridge/config_cluster.py:266` | 2026-09-05 |
| a declared max or crit is sanity-bounded rather than trusted, because three of the ten max-declaring sensors declare 65261.85 (0xFFFF sentinel, an undeclared max encoded as a number): daniel-server nvme temp2/temp3 and daniel-box nvme temp3. Ratio-of-max against that is unreachable, so those sensors would read green through a fire — the inert-check class in [[an-optimisation-can-land-green-and-be-inert]]. A declared value outside (HWMON_TEMP_MIN_PLAUSIBLE_C, HWMON_TEMP_MAX_PLAUSIBLE_C] is therefore treated as UNDECLARED and falls through (crit, then arm 2). This is also why the fallback arm is not optional: without it, 14 of 21 sensors — including BOTH daniel-pi sensors, on the host with no fan — carry no limit at all. | `ansible/roles/k8s/monitor-bridge/files/bridge/config_host.py:196` | 2026-09-05 |
| daniel-box's k10temp/Tctl takes its limit` marker in verdicts.host.hwmon_temp_limits for that evidence and for how to fetch amd.com, and the | `ansible/roles/k8s/monitor-bridge/files/bridge/config_host.py:215` | 2026-09-05 |
| k10temp subtracts no Tctl offset` marker above it for why Tctl is the junction temperature this compares (#1003, #1152). | `ansible/roles/k8s/monitor-bridge/files/bridge/config_host.py:217` | 2026-09-05 |
| 12 cycles (60 min at INTERVAL=300), not the 3 (15 min) this shipped with — daniel-box's k10temp/Tctl boosts into the low 90s as ordinary workload, and 3 cycles paged on 26 of those excursions in a week. Measured 2026-09-06 against Prometheus over a true 7 d (retention here is ~11.4 d, so a [30d] query silently returns ~11 d — #1314), sampling `node_hwmon_temp_celsius{chip="pci0000:00_0000:00:18_3",origin="daniel-box"}` at the 5 min loop cadence: 12.0% of samples above the 90C limit, p50 52.875C, p95 93.125C, max 93.75C against AMD's rated 100C. 115 separate excursions above 90C, run lengths in cycles: 66x1, 23x2, 12x3, 5x4, 2x5, 3x6, 3x7, 1x8, then ONE of 18 (90 min, starting 2026-09-03T12:20Z). Nothing between 9 and 17 cycles — 12 sits in that empty gap, 1.5x the largest ordinary excursion and half the outlier. Pages per week by threshold: 3 -> 26, 5 -> 10, 8 -> 2, 9 -> 1, 12 -> 1, 19 -> 0. 12 keeps the ONE 90-minute excursion paging, deliberately: 90 min pinned above 90C is the shape of a cooling fault, and a threshold chosen to silence it would be fitted to the observed maximum with no margin left to detect one. The operator's reading is that the excursions themselves are normal (issue #1186) — every one of them falls in the 11:00-03:20Z daytime band with an 8 h overnight hole, so this is scheduled and interactive work rather than an idle-state thermal floor, which is what makes hysteresis the remedy rather than cooling or a wider HWMON_TEMP_RATIO. Estate-wide is acceptable because no other sensor is near its limit: over the same 7 d, daniel-server's coretemp peaked at 74C against a 90C limit and daniel-pi's thermal zone at 79.5C against the 85C fallback, so this delays nothing that is currently firing. Caveat: _down_streaks is module-global and resets on a bridge restart, so a deploy mid-excursion costs a full 60 min re-accumulation. That cuts toward quiet, not toward a missed fault. | `ansible/roles/k8s/monitor-bridge/files/bridge/config_host.py:228` | 2026-09-06 |
| 3000, from the gap between churn and bursts rather than from the streak the issue proposed. Measured 2026-09-11 over the 14 days Prometheus retains, at 1h resolution: the client-side counter is non-zero in 26 of ~336 hours, 13 of those clear 1000, and the ONLY samples between 1000 and 6261 are the two that held this monitor red for 51 minutes on 2026-09-03 at 1020. Every other real loss starts at 6261 and runs to 90,724 client-side and 216,545 server-side. So churn tops out at ~1020 and the smallest genuine burst is 6261 — 3000 sits 2.9x above the first and 2.1x below the second, which is a derived midpoint rather than a number fitted to the episode. A 2-3 cycle streak was rejected on its own evidence: that episode ran 11 cycles, so a streak would have removed 3 of them and left the tile red for 40 minutes. | `ansible/roles/k8s/monitor-bridge/files/bridge/config_io.py:319` | 2026-09-11 |
| 8 days, DERIVED from the drill's cadence rather than picked round. is k3s_etcd_restore_drill_cron = "20 10 * * 1" — weekly, Monday 10:20 — so anything over 7 days means a scheduled run did not happen or did not pass. 8 gives one day of slack for the run itself and for a check that evaluates just before the window, and no more: a wider grace and the monitor clears the very miss it exists to catch, which is how a 24h grace against a 23h gap read green on 2026-08-25. Move this ONLY together with the cron; the two are pinned to each other by test_etcd_drill_grace_is_derived_from_the_cron. | `ansible/roles/k8s/monitor-bridge/files/bridge/config_service.py:201` | 2026-09-05 |
| cAdvisor metrics must NOT go through origin_sel(). | `ansible/roles/k8s/monitor-bridge/files/bridge/net.py:50` | 2026-09-01 |
| an EXCLUDE, never origin_sel(). | `ansible/roles/k8s/monitor-bridge/files/bridge/net.py:84` | 2026-09-01 |
| this gate does NOT go through _gate() — the reuse branch below sits between the check_enabled() test and the log/push, which is exactly the span _gate() owns. Threading a precomputed verdict through would add a parameter for one caller and hide the reuse. | `ansible/roles/k8s/monitor-bridge/files/check.py:128` | 2026-08-23 |
| folded into `memory` rather than given its own Kuma monitor, for the reason recorded at with_pi_ports — a new monitor costs a new push token in SOPS and a monitor created by hand in the Kuma UI. This monitor already owns "this box is running out of memory", and a cgroup taking the box is exactly that. | `ansible/roles/k8s/monitor-bridge/files/checks/host.py:202` | 2026-09-06 |
| the message leads with the cgroup when the arm fires, like with_pi_ports, because "memory DOWN" otherwise sends someone to look at node_memory_* when the fault is one cgroup's reclaim. | `ansible/roles/k8s/monitor-bridge/files/checks/host.py:206` | 2026-09-06 |
| the queries filter by METRIC, not by cgroup. absence is a fault, not the set that is judged — so a cgroup that appears later (user-1000-slice exists only once someone has logged in since boot) is covered the moment it reports, without its absence paging. | `ansible/roles/k8s/monitor-bridge/files/checks/host.py:209` | 2026-09-06 |
| a down_streak, and it does double duty — see CLAUDE_CGROUP_CONSECUTIVE in bridge/config_host.py for both jobs (burst suppression and the weekly cgroup-recreate counter reset). | `ansible/roles/k8s/monitor-bridge/files/checks/host.py:213` | 2026-09-06 |
| the fetch arrives as an ARGUMENT (`prom_vector`, threaded from check_mem) rather than being reached through `bridge.net`, the same shape check_pi_pressure gives `tcp_open`. A test hands in a fake that answers each of the two queries separately; patching `bridge.net.prom_vector` would answer both with one value, which is how a fixture ends up proving the opposite of what it claims. | `ansible/roles/k8s/monitor-bridge/files/checks/host.py:216` | 2026-09-06 |
| neither query is a subquery. CLAUDE_CGROUP_STALL_MAX_PCT was derived with a `[6h:1m]` subquery, which is far more expensive than what runs here; measured against the live Prometheus three times each on 2026-09-06, both queries below returned in 0.125-0.139s end to end, including process start and the Traefik hop this check does not pay. That is the PR #482 trap — an exploration query measured in place of the production one. | `ansible/roles/k8s/monitor-bridge/files/checks/host.py:221` | 2026-09-06 |
| Grouped by origin as well since 2026-09: daniel-server reports the same cgroup names. | `ansible/roles/k8s/monitor-bridge/files/checks/host.py:227` | 2026-09-09 |
| TCP connect is the whole signal. to its container's Docker state through glances' /api/4/containers, fetched only after the connect failed — measured 2026-08-27 that endpoint took 4.43s and then TIMED OUT at the 10s HTTP_TIMEOUT on the very next call, so polling it every cycle would have left the arm failing open most of the time. glances retired (#2004) and nothing the cluster can reach serves that view (docker-proxy publishes no port), so the verdict names the port and both causes and the operator reads `docker ps` on the Pi. The verdict never depended on the attribution, so the arm loses a diagnosis and keeps its page. | `ansible/roles/k8s/monitor-bridge/files/checks/host_edge.py:49` | 2026-09-18 |
| the message leads with the dead ports when the arm fires, because "pi_pressure DOWN" otherwise pages someone to look at load and memory when the fault is neither. Same shape as with_ha_ban putting the ban first. | `ansible/roles/k8s/monitor-bridge/files/checks/host_edge.py:57` | 2026-09-18 |
| a down_streak, unlike with_ha_ban's arm. their ports are legitimately closed for a few seconds and a single cycle can read dead. A detached container persists until someone recreates it, so it survives the grace. | `ansible/roles/k8s/monitor-bridge/files/checks/host_edge.py:60` | 2026-09-05 |
| this arm rides inside a PROM_DEPENDENT check since 2026-09-18, so a Prometheus outage or a dead Pi node_exporter skips the port probes with the pressure arms. Both cases already page (Prometheus Reachable / Scrape Targets), and the Kuma HTTP monitor on wg-easy still watches the one Pi port a person uses. A monitor of its own would cost a push token in SOPS, the reason the arm was folded in here to begin with. | `ansible/roles/k8s/monitor-bridge/files/checks/host_edge.py:63` | 2026-09-18 |
| the check cannot tell a failing Ookla run from a stopped scheduler, and this repo accepts that (#1603). speedtest-tracker writes NO row for a failed run — that is the LinuxServer image's own behaviour, decided between the Ookla CLI exiting non-zero and the results table, and the role has no hook in between. Measured 2026-09-10 against the live app: 25 rows, ids 825-849, every one `status: completed`, so speedtest_verdict's `status != "completed"` branch is reachable by test and has never been taken by data. The two rejected alternatives are a LogQL check for the per-tick log line (Loki does not retain the pod's logs across the restart that is exactly when it is wanted) and replacing | `ansible/roles/k8s/monitor-bridge/files/checks/host_edge.py:168` | 2026-09-10 |
| ` on the SPEEDTEST_SCHEDULE line — it moves the transcript, not the reliability). What mitigates it is the staleness message naming both hypotheses and asserting neither, in verdicts/host.py's speedtest_verdict. | `ansible/roles/k8s/monitor-bridge/files/checks/host_edge.py:176` | 2026-09-10 |
| fails OPEN on a Loki error instead of adding ha_heartbeat to LOKI_DEPENDENT. Membership there suppresses the WHOLE check during a Loki outage, which would blind the real heartbeat — trading a live wedge-detector for a secondary arm is the wrong way round. The ban arm also skips down_streak: down_streak exists to ride out a transient, and a ban is a discrete event that either happened in the window or did not — a second cycle's confirmation would add nothing. Note this arm reports the ban EVENT, not the ban STATE: it self-clears HA_BAN_WINDOW after the ban is issued even though the entry survives in /config/ip_bans.yaml. See the HA_BAN_WINDOW comment for why that is the only signal available. | `ansible/roles/k8s/monitor-bridge/files/checks/service.py:325` | 2026-09-01 |
| `env` reaches `load_config` and `registry.build_checks`, and nothing else. Slice 17b moved the registry out of `check.py` precisely so `main(env={...})` can decide which monitor a result is pushed to, which it could not while the KUMA_PUSH_* tokens were read at import into a module-level list. The FOUR reachability-gate tokens are the remaining exception: `gates._gate` still reads them from `os.environ` at push time, so `env` does not redirect a gate's push. They are four literals in `check.run_once` rather than a table, so there is no module global left to remove — moving them would be a new parameter for one caller, not the same fix. | `ansible/roles/k8s/monitor-bridge/files/cli.py:97` | 2026-09-05 |
| floor 20 pods, from measurement rather than intuition. the pre-filter counts never fell below 98 (restarts), 98 (oom) and 70 (cpu, which sees only pods carrying a cpu limit); live values were 99/99/71. Count the DIVISION for the cpu arm, not its denominator: check_cpu_throttle floors `ratio_vec`, and a PromQL binary op drops series present on only one side, so the two can differ. They happened to agree here — both 71 live, 70 at the trough — which is exactly why the easier query is the wrong one to re-measure from. 20 sits 3.5x under the smallest observed trough, so a deploy draining pods cannot reach it, while a broken selector or a vanished kubernetes-cadvisor job lands at 0. One floor for all three, not one each: a floor applied to two of three would reproduce the selector-drift class inside its own fix. | `ansible/roles/k8s/monitor-bridge/files/verdicts/cluster.py:64` | 2026-08-27 |
| k10temp subtracts no Tctl offset on daniel-box, so there is no lower series to prefer over the Tctl this arm caps at the flat fallback (issue #1003; #995 established only that k10temp declares no max/crit here). The driver sets `temp_offset` ONLY on a `tctl_offset_table` hit and reports `Tdie = get_raw_temp() - temp_offset`. Read out of the module this host actually runs — `strings` over /lib/modules/6.8.0-138-generic/kernel/drivers/hwmon/k10temp.ko.zst, confirmed against drivers/hwmon/k10temp.c at tag v6.8 — that table holds six family-0x17 SKUs (Ryzen 1600X/1700X/1800X/2700X, Threadripper 19xx/29xx). Both halves of its match fail for `AMD Ryzen 7 8845HS w/ Radeon 780M Graphics` (family 25, model 117): the family is not 0x17, and no entry string is a substring of that model id. So `temp_offset` stays 0, and a Tdie here would be NUMERICALLY IDENTICAL to Tctl — reading it would be a no-op on the same number, not a correction. Live sysfs agrees: hwmon2 carries temp1_input and temp1_label (Tctl) alone, no Tdie, no Tccd, no max, no crit. This settles the offset and NOT the number, which is #1152 and is now settled too: | `ansible/roles/k8s/monitor-bridge/files/verdicts/host.py:244` | 2026-09-05 |
| daniel-box's k10temp/Tctl takes its limit from AMD's own published Tjmax of 100C via HWMON_TEMP_RATED_MAX_C, not from this flat fallback — `Max. Operating Temperature (Tjmax)` / `100°C` on amd.com's Ryzen 7 8845HS product page, matching this host's /proc/cpuinfo model name. **amd.com is reachable from here; a plain fetch is what is not** — #1003, #1152 and #1158 each recorded it as timing out, but with a browser User-Agent it returned 200 in 0.66s on 2026-09-05. Try the UA before concluding the page is unreachable. This role's CLAUDE.md holds the rest: why 90C is the same limit daniel-server's coretemp already carries, and the 12.0% of a true 7 days (measured 2026-09-06; [30d] here truncates to ~11.4, #1314) daniel-box still spends above it — which #1186 answered with hysteresis, not a wider ratio. | `ansible/roles/k8s/monitor-bridge/files/verdicts/host.py:261` | 2026-09-05 |
| ` marker in hwmon_temp_limits. | `ansible/roles/k8s/monitor-bridge/files/verdicts/host.py:293` | 2026-09-05 |
| name both hypotheses, assert neither (#1483). row and a stopped scheduler writes no row, so this arm cannot tell them apart — evidence in test_speedtest_stale_message_names_both_causes_and_asserts_neither. | `ansible/roles/k8s/monitor-bridge/files/verdicts/host.py:369` | 2026-09-10 |
| 3 GiB even though 85% of the 20Gi claim also breaches at 3 GiB free. sized to the transient, not to open a gap under the percentage: a resize moves what 85% means and leaves this where the copy is, which at the 10Gi the incident happened on was 1.5 G more warning than the percentage gave. | `ansible/roles/k8s/monitor-bridge/templates/env-secret.yaml.j2:174` | 2026-09-17 |
| 12 cycles` marker in files/bridge/config_host.py. | `ansible/roles/k8s/monitor-bridge/templates/env-secret.yaml.j2:261` | 2026-09-06 |
| stays at 20, and home-assistant stays OUT of LOG_ERROR_IGNORE. raising this or ignoring that container, after `fatal log lines in 1h: home-assistant (23)` and `(26)` held k3s Workload Health red for 46 and 43 minutes. Both were declined on the measurement: over the 4 days to 2026-09-11 this selector and pattern matched home-assistant ZERO times, so the burst is episodic rather than chronic, and a permanent ignore would blind the arm on a container that is normally silent. The 7d query times out against Loki, so "no matches in 4d" cannot see the 09-02/03 burst itself — it establishes that HA is not chronically noisy, NOT that those lines were benign. Raise this only from a window that contains a burst and a reason to call it ordinary. | `ansible/roles/k8s/monitor-bridge/templates/env-secret.yaml.j2:372` | 2026-09-11 |
| ` markers in verdicts.host). | `ansible/roles/k8s/monitor-bridge/tests/test_host_temp.py:147` | 2026-09-05 |
| ` markers in verdicts.host). | `ansible/roles/k8s/monitor-bridge/tests/test_host_temp_rated.py:6` | 2026-09-05 |
| 12 cycles` marker in bridge/config_host.py" | `ansible/roles/k8s/monitor-bridge/tests/test_host_temp_rated.py:187` | 2026-09-06 |
| permanent posture (operator 2026-08-14) — with ETP Local load-bearing for client IPs, announcement and placement stay on one node as a unit; this pin only ever moves together with the L2Advertisement nodeSelector. | `ansible/roles/k8s/mosquitto/templates/deployment.yaml.j2:26` | 2026-08-27 |
| 2 hops, and it must not drop to 1. routing; it makes n8n attribute every request to the wrong client IP, which is what its login throttling and audit log key on. The two hops on a public request are Cloudflare and the cluster Traefik — Cloudflare's ranges sit in `forwardedHeaders.trustedIPs` on both entrypoints (see the traefik role's cloudflare-ip-drift.sh.j2), so it is a trusted proxy that sets X-Forwarded-For and counts. This is NOT a leftover from the Docker edge Traefik uninstalled 2026-08-14; that host was never one of these two. An earlier version of this comment said the opposite and invited exactly the change that breaks it. | `ansible/roles/k8s/n8n/templates/deployment.yaml.j2:107` | 2026-08-22 |
| the five probe blocks below stay hand-copied — do not fold them into an `include_tasks` loop. Tried 2026-08-23: it works and it shrinks this file from 955 lines to 470, with ansible-lint green at production profile. It also breaks `test_k8s_autodeploy_guard.py::test_auto_deployable_roles_gate_every_batch_workload_they_render`. That test's `_batch_gated_names` matches each rendered Job name against a LITERAL `wait --for=condition=complete job/<name>` in the task text. Under a loop the wait reads `job/netpol-baseline-probe{{ probe.suffix }}`, which the scanner cannot match: the wait still runs, but the check that catches a genuinely ungated probe Job stops working for this role. N Jobs need N literal wait statements, so the duplication is the guard's substrate — and planting literal names somewhere the scanner happens to read would be a dodge, not a fix. Same class as textual-guard-checks-break-on-indirection. | `ansible/roles/k8s/netpol-baseline/tasks/main.yml:154` | 2026-08-23 |
| ` marker at `FINALDELAY` in `roles/setup/nut_host/templates/host-upsmon.conf.j2`. | `ansible/roles/k8s/nut/CLAUDE.md:49` | 2026-09-17 |
| these apt packages resolve at build time. archive supersedes it (the pool keeps only the current deb) and nothing bumps it, and a snapshot mirror freezes security updates with no Renovate datasource to raise a PR. The base digest above is the pin; #2211 holds the evidence and the operator's decision. | `ansible/roles/k8s/nut/templates/Dockerfile.j2:7` | 2026-09-22 |
| apk packages resolve at build time — an `=version` pin 404s once the branch supersedes it and nothing bumps it; the base digest is the pin. See #2211. | `ansible/roles/k8s/pi-peer-backup/templates/Dockerfile.j2:13` | 2026-09-22 |
| permanent posture (operator 2026-08-14) — with ETP Local load-bearing for client IPs, announcement and placement stay on one node as a unit; this pin only ever moves together with the L2Advertisement nodeSelector. BOTH instances pin here, deliberately. Every VIP is announced from a single node (setup/k3s metallb-pool.yaml.j2, marked PERMANENT), and externalTrafficPolicy: Local means a pod on the other node would receive nothing. Redundancy here is against a rollout, not against node loss. | `ansible/roles/k8s/pihole/templates/deployment.yaml.j2:34` | 2026-08-27 |
| 780s, the most the rollback budget allows. the snapshot, the revert, the netpol probe's own 300s wait, this wait and the stabilisation soak inside gitops_deploy_k8s_rollback_timeout_s (1620s), and those other terms take 840s. test_k8s_rollback_budget_covers_the_worst_single_promoted_service enforces that sum, and counts the probe wait since #2399. 780s leaves 245s over the one measured pull. A slower link needs the rollback budget raised first, and that budget is already at the ceiling the four tree-lock waiters clear. A cached image rolls in seconds, so the budget costs time only on a real hang. | `ansible/roles/k8s/prowlarr/defaults/main.yml:44` | 2026-09-24 |
| 0700 owned by sys_user, not 0755 — the script interpolates the qBittorrent WebUI password directly (same shape as janitorr-health.sh.j2's Kuma token), and at 0755 any local account could read it. | `ansible/roles/k8s/qbittorrent/tasks/main.yml:81` | 2026-08-27 |
| no nodeSelector — an intermittent ~34s here is the price of proving both nodes. | `ansible/roles/k8s/registry/templates/selftest-push-job.yaml.j2:32` | 2026-08-22 |
| the schedule stays INTERNAL to the app rather than becoming a k8s CronJob. #1483 raised the CronJob because nothing outside the container can see a tick fire. Measured 2026-09-10, the internal scheduler is not the flaky part: rows 825-849 land on :00 at 05/11/17/23 UTC every day, with exactly ONE miss — 2026-09-09 17:00, on a day the pod was rescheduled by the 20:12 daniel-box reboot. A CronJob would move the transcript, not the reliability, and it cannot write into the app's own results table without reimplementing the Ookla run. Revisit if a miss ever occurs without a pod restart beside it. | `ansible/roles/k8s/speedtest/templates/deployment.yaml.j2:63` | 2026-09-10 |
| permanent posture (operator 2026-08-14) — with ETP Local load-bearing for client IPs, announcement and placement stay on one node as a unit; this pin only ever moves together with the L2Advertisement nodeSelector. | `ansible/roles/k8s/terraria/templates/deployment.yaml.j2:27` | 2026-08-27 |
| ` comment in `static-config.yaml.j2` has why. | `ansible/roles/k8s/traefik/CLAUDE.md:90` | 2026-09-24 |
| these are one-way for a cluster that has already deployed with them on. name from manifests_files deletes the STAGED file (manifests/tasks/main.yml:148) but never the live object — see kubectl-apply-leaves-orphaned-objects. Flipping either to false on prod therefore needs a hand `kubectl delete`, which the manifest-prune-check cron flags. Nothing does that today; the flags exist so a new cluster can come up without the subsystem, not so prod can shed it. Namespaces this cluster's Traefik watches. rbac.yaml.j2 renders one namespaced Role per entry, and static-config.yaml.j2 lists the same set as `providers.kubernetesCRD.namespaces`. ONE variable, so the two cannot drift — they were two hand-maintained lists with a comment on each asking the next editor to keep them in step. ENFORCED by ansible/tests/services/test_traefik_watched_namespaces.py, which compares the RENDERED manifests (Role namespaces vs the provider list), not the templates' text. | `ansible/roles/k8s/traefik/defaults/main.yml:35` | 2026-08-28 |
| role-local, and paired with authelia_k8s_manage_crowdsec rather than shared. variable in group_vars/all.yml would be an `ansible/inventory/` path, which the GitOps deployer treats as broad — it defers and returns WITHOUT fast-forwarding, so the change would need clearing by hand. The two surfaces are also asymmetric: Traefik carries a bouncer plugin and a Middleware and ENFORCES bans, where Authelia carries only an agent and produces the failed-login lines the scenarios read. | `ansible/roles/k8s/traefik/defaults/main.yml:73` | 2026-08-28 |
| a flag, not generated staging tokens. generated values would render harmlessly — and that was wrong twice. The routers name backends a staging cluster does not run, so it is the same class as CrowdSec rather than an inert Secret. And staging's secrets file is encrypted to daniel-server's key alone and holds exactly one key by design (Decision 5), so minting tokens into it is operator work on that host, not something a deploy can arrange. | `ansible/roles/k8s/traefik/defaults/main.yml:85` | 2026-08-28 |
| LAN-only. `traefik.<domain>`, and that host is deliberately NOT reproduced here — the dashboard enumerates every router, service and middleware on this edge, which is a map of the whole homelab, and Authelia in front of it does not make publishing that map worthwhile. This route ignores k8s_public_route for the same reason prometheus-monitoring does: `public=false` is the convention for infrastructure APIs (see ansible/templates/ingressroute.yml.j2's macro header). WireGuard peers still reach it — they resolve `.local.` names through Pi-hole — because the gate here is the hostname, not a ClientIP matcher. | `ansible/roles/k8s/traefik/templates/dashboard-ingressroute.yaml.j2:28` | 2026-08-24 |
| /api is deliberately NOT routed, and the dashboard SPA is knowingly non-functional as a result (issue #951). `api@internal`'s `/api/http/routers` prints every router's `rule` string verbatim — Traefik v3.7.11 does not redact it — and two file-provider routers in livesync-gate-secret.yaml.j2 embed `livesync_sync_token` and the `homelab_mcp_token` bearer in `Header()` matchers. A Host-only match with no PathPrefix at all (the shape this block carried before #951) routes that same `/api` surface, which is exactly how those two credentials were readable behind a one_factor Authelia session. The upstream-documented form `(PathPrefix(`/dashboard`) \|\| PathPrefix(`/api`))` is rejected for the same reason. This route exists only so `traefik.local.<domain>` does not fall through to a default backend — restoring `/api` re-exposes both credentials to any Authelia session, so don't add it back to make the SPA work again. | `ansible/roles/k8s/traefik/templates/dashboard-ingressroute.yaml.j2:38` | 2026-09-03 |
| permanent posture (operator 2026-08-14) — with ETP Local load-bearing for client IPs, announcement and placement stay on one node as a unit; this pin only ever moves together with the L2Advertisement nodeSelector. | `ansible/roles/k8s/traefik/templates/deployment.yaml.j2:27` | 2026-08-27 |
| the shared emptyDir does not need clearing between container starts (#1345). A failing startupProbe restarts the CONTAINER, so /plugins-storage is the same emptyDir on every retry, where the #1322 repair was a rollout restart onto a fresh one. Measured 2026-09-10 on this exact image: each restart re-downloads. Traefik clears the sources tree at every process start and re-requests the archive on every install, so a stale or partial archive cannot short-circuit recovery. Full evidence in this role's CLAUDE.md; do not re-run that outage. | `ansible/roles/k8s/traefik/templates/deployment.yaml.j2:319` | 2026-09-10 |
| ` marker on the nodeSelector in deployment.yaml.j2: announcement and placement move as a unit, permanently. ETP Local is what makes that binding — a VIP-backed pod on the non-announcing node makes the announcer drop all forwarded LAN traffic. | `ansible/roles/k8s/traefik/templates/service.yaml.j2:32` | 2026-08-28 |
| Cloudflare only, and not `lan_subnet` ({{ lan_subnet }}). was trusted for the Docker-era edge (33bdbdb32, narrowed from 10.0.0.0/8 by f0429fb4b), where the Docker bridges and co-hosted proxies sat on it. On k3s nothing on the LAN proxies to Traefik: neither node runs Docker, and the Pi runs only docker-proxy, wg-easy, alloy and autoheal. Trusting the /24 let any LAN device forge XFF, and that included every WireGuard peer. wg-easy masquerades its `wg_client_subnet` ({{ wg_client_subnet }}) peers to the Pi's LAN address, which is inside the /24. With the /24 untrusted, Traefik strips a LAN client's XFF and forwards the connection address, so Authelia `networks:` rules and ClientIP() matchers still see the LAN address. Not the pod CIDR either: the Service runs externalTrafficPolicy: Local so requests arrive with the real client address rather than cni0's, and trusting the pod network would let any pod forge XFF. If a LAN reverse proxy is ever added in front of Traefik, list its single address here rather than the /24. | `ansible/roles/k8s/traefik/templates/static-config.yaml.j2:33` | 2026-09-24 |
| keep it, and expect one or two error lines per container start. logs | `ansible/roles/k8s/traefik/templates/static-config.yaml.j2:66` | 2026-09-06 |
| ` marker sits at | `ansible/roles/k8s/uptime-kuma/CLAUDE.md:261` | 2026-09-19 |
| ` marker above the drift tiles in `templates/static-monitors.yaml.j2` keeps them. | `ansible/roles/k8s/uptime-kuma/CLAUDE.md:291` | 2026-09-19 |
| `delete` stays, with the blast radius it has (#2076, 2026-09-19). entity that fails to PARSE counts as removed, and a monitor whose reference (notification_name_list, tag_names, parent_name) does not resolve fails to parse too — so one bad shared entity deletes every monitor that names it, with its history. It did, twice: 107 monitors on 2026-09-18 (a notification Tera could not parse) and 23 on 2026-09-19 (tags referenced by display name, not id). Automatic pruning is kept because a retired declaration otherwise outlives its file forever (monitors 300/301 after E7), and history here is in the no-backup tier by design. What holds the line instead is guards on the declarations: every reference resolves to a declared entity id, no Tera syntax reaches an entity value outside a raw block, and applyExisting is declared — ansible/tests/services/test_kuma_static_monitors.py and test_kuma_tags_and_descriptions.py. Full account in this role's CLAUDE.md. | `ansible/roles/k8s/uptime-kuma/templates/deployment.yaml.j2:145` | 2026-09-19 |
| no readinessProbe here, and none is possible. containers, so any readinessProbe on /health re-creates exactly the 76s Service gap this change removed — the same reason recorded for this container in ansible/tests/k8s/test_readiness_coverage.py. The cost of the swap is that initialDelaySeconds is a fixed grace where a startupProbe was adaptive: a container that never becomes healthy is now killed at ~360s rather than ~300s. Liveness here protects a reconcile loop whose death breaks no monitoring, so 60s is affordable. | `ansible/roles/k8s/uptime-kuma/templates/deployment.yaml.j2:195` | 2026-09-06 |
| 360s, not 300 — the deadline must exceed the producer's PERIOD, and the period is the cron plus the work. Both DDNS pods run the image's default UPDATE_CRON=@every 5m and ping Kuma at the END of each cycle, after IP detection and the record check, so a beat lands at 300s + that cycle's work. The image caps the work at 5s (IP detection) + 30s (record updating), so the pathological beat is 335s and 360 leaves 25s of margin — not the 60s the round number suggests. Re-derive from those two timeouts, not from the observed few seconds, if the image's defaults ever move. At interval 300 it raced every cycle and lost ~7 times a day: 47 + 40 flap episodes over the 7 days to 2026-08-23, each a ~2-minute dip with nothing actually wrong. Fixed here rather than by setting UPDATE_CRON=@every 4m in both deployments — that edits two files and makes the Cloudflare API busier to cure a monitoring artifact. Raise this if the cadence changes. | `ansible/roles/k8s/uptime-kuma/templates/static-monitors.yaml.j2:249` | 2026-08-23 |
| 5400s, not the 1200 its host-cron siblings use — the deadline must exceed the PRODUCER'S PERIOD, and this producer is an hourly cron (k3s_remember_logs_cron_minute), not a */10 one. 3600s plus the run leaves 5400 with 30 min of margin. At 1200 it would fire DOWN every hour with nothing wrong, which is the failure the Cloudflare DDNS entries above record. Move this and the cron minute together or neither. | `ansible/roles/k8s/uptime-kuma/templates/static-monitors.yaml.j2:347` | 2026-08-24 |
| the one-period-plus-an-hour margin on the drift tiles below (Docs Refresh, Manifest | `ansible/roles/k8s/uptime-kuma/templates/static-monitors.yaml.j2:372` | 2026-09-17 |
| 100800s (28h), not the 90000 the other daily tiles use — the beat lands at the END of the run, and a fast run today followed by one that draws the full 10-min jitter and runs to the 100-min unit timeout tomorrow spaces two beats 25h50m apart; 25h fires DOWN on that. 28h leaves 2h of margin and stays under two periods, so a missed run still reports. test_renovate_agent_unit.py pins both bounds. Gated on the token so a checkout without the secret renders no tile rather than one that sits red. | `ansible/roles/k8s/uptime-kuma/templates/static-monitors.yaml.j2:573` | 2026-09-03 |
| ` block at `valheim_k8s_server_size` in `defaults/main.yml` has the numbers. | `ansible/roles/k8s/valheim/CLAUDE.md:41` | 2026-09-17 |
| 20Gi, not the 10Gi it was until 2026-09-17, because the volume holds THREE copies of the install at rest and a FOURTH during every game update. The image keeps steamcmd's own tree (dl/server), the rsync'd vanilla copy (server) and the BepInEx merge (bepinex), which with the three-day zip rotation measured 7.7 G steady on 2026-09-17. A Steam update then rsyncs server + the BepInEx pack into bepinex.tmp before swapping it in, so the merge needs one more install's worth free. At 10Gi that was 2.1 G free against a 2.2 G copy: the l-1.0.14 update on 2026-09-17 hit ENOSPC four times in an hour, the hourly world zip failed alongside it, and the PVC Fullness tile went DOWN (#1866). Longhorn thin-provisions, so the unused half costs nothing on disk; it exists so the peak sits near 55% rather than 100%. longhorn-nobackup allows expansion, so this line grows the live claim in place. | `ansible/roles/k8s/valheim/defaults/main.yml:217` | 2026-09-17 |
| apk packages resolve at build time — an `=version` pin 404s once the branch supersedes it and nothing bumps it; the base digest is the pin. See #2211. | `ansible/roles/k8s/valheim/templates/Dockerfile.j2:13` | 2026-09-22 |
| nothing self-heals a volume this attach leaves behind. an API call fails, the volume stays attached with `disableFrontend: true` and the workload at zero replicas (CLAUDE.md's manual-recovery steps), and the next deploy's k8s/volume-claim seed pod mounts the same RWO claim — whether that mount succeeds, hangs or fails against a non-pod maintenance attachment is untested, because nothing in this repo exercises a real Longhorn revert. Accepted: treat a stuck maintenance-mode attach as blocking that service's next deploy until cleared by hand, not as something the pipeline routes around. Long form: docs/gitops-pipeline.md, *The deployer's record*. | `ansible/roles/k8s/volume-revert/tasks/claim.yml:243` | 2026-09-21 |
| this gate FAILS OPEN, and that is not an oversight. value and the fleet default (only jellyfin-config carries a cap today), an absent field reads empty, and a listing that matches nothing returns rc 0 with empty stdout — every one of those renders a cap of 0 and the fail task below does not fire. A gate in front of all 13 opted-in services, guarding a hazard nobody can reach today (16 GiB cap against ~1.9 GiB in use, measured 2026-09-10), must never turn a bad read into a fleet-wide deploy stop. It fires only where the numbers positively say the cap is full. | `ansible/roles/k8s/volume-snapshot/tasks/claim.yml:133` | 2026-09-10 |
| this warning is an Ansible debug line and nothing forwards it to Discord. not fixed. Under the GitOps deployer the play output reaches nobody, and a later rollback's revert-status note (gitops_deploy's `rollback_volume_revert_note`) names a service as one the revert targets by reading its role DEFAULTS (`k8s_autodeploy_snapshot_pvcs`), not what this task did on THAT run — so an operator reading the rollback alert has no signal that this snapshot never happened, and no reason to expect the revert that follows to fail loudly rather than do nothing. Closing it means threading a per-run "snapshot skipped" fact from this play into the deployer's alert. Long form: docs/gitops-pipeline.md, *The deployer's record*. | `ansible/roles/k8s/volume-snapshot/tasks/claim.yml:679` | 2026-09-21 |
| ` marker in | `ansible/roles/k8s/wg-easy/CLAUDE.md:74` | 2026-09-24 |
| a one-shot init container, not a re-asserting sidecar. its SQLite DB (no env override), and upstream v15.4.0's default PostUp APPENDS `-A FORWARD -i wg0 -j ACCEPT` (migration 0001) while PostDown deletes exactly those rules; its firewall step (src/server/utils/firewall.ts) only ever touches the WG_CLIENTS chain and its jump. So a DROP inserted at position 1 before wg-easy starts stays above the ACCEPT through startup, a container restart (the pod netns survives it) and the UI's Restart Interface; a pod recreate reruns this. Same image as the main container on purpose: it selects iptables-legacy via update-alternatives, and another image could write the rules into the nft tables wg-quick never reads. Enabling per-client filtering in the UI inserts the WG_CLIENTS jump ABOVE these rules. | `ansible/roles/k8s/wg-easy/templates/deployment.yaml.j2:79` | 2026-09-24 |

## roles/setup

| Marker | File | Decided |
|---|---|---|
| worktree, not same-dir. thing this repo's parallel-session rule forbids, and `prune_worktrees.py` already collects finished worktrees while respecting a live session's lock. | `ansible/roles/setup/claude_code/defaults/main.yml:21` | 2026-08-23 |
| auto, matching interactive sessions on this host. every tool call, which from a phone means a session that stops constantly. Falling back to `default` is the supported answer if auto degrades without a TTY — answering prompts remotely is what Remote Control is for. | `ansible/roles/setup/claude_code/defaults/main.yml:26` | 2026-08-23 |
| 10, not the CLI's default of 32. at 200-400 MB RSS, so 10 sessions ask for up to ~4 GB against ~15 GB available with k3s already running. Raised from 3 to 10 on 2026-09-04 at user request. | `ansible/roles/setup/claude_code/defaults/main.yml:32` | 2026-09-04 |
| 12G on the shared parent, with the per-plane caps below left at 8G as sub-bounds. Lowering both planes to 6G would also make the sum 12G, and is the worse shape: a plane would throttle at half the box's fleet budget while the other plane sat idle. A parent ceiling shares elastically — either plane may take up to its own 8G, and the two together are held to 12G by the parent. | `ansible/roles/setup/claude_code/defaults/main.yml:91` | 2026-09-06 |
| a separate timer + `systemctl try-restart`, NOT RuntimeMaxSec= on claude-rc.service. RuntimeMaxSec expiry is recorded as a failure (result: timeout), so it would fire OnFailure=claude-rc-alert.service and page Discord once a week on a healthy host — training the alert to be ignored, which is worse than no alert. | `ansible/roles/setup/claude_code/templates/claude-rc-restart.service.j2:6` | 2026-08-23 |
| this unit lives in {{ claude_code_fleet_slice }}, not the default system.slice, so that it shares ONE parent cgroup with the other half of the Claude fleet — the login-session plane in user-{{ claude_code_login_uid }}.slice. Until 2026-09-06 both planes carried claude_code_rc_memory_high independently, which reads as one 8G bound and was two: siblings under different parents, neither seeing the other's usage, so the fleet could reach 16G of anon plus 4G of swap before either throttled. Issue #1264. The single bound now sits on {{ claude_code_fleet_slice }} (templates/fleet-slice-caps.conf.j2) and MemoryHigh below is this plane's sub-bound under it. | `ansible/roles/setup/claude_code/templates/claude-rc.service.j2:16` | 2026-09-06 |
| the background-shell pressure reaper is turned OFF here. `process.on("memoryPressure", ...)` and, when Node emits that event, kills every running backgrounded Bash task with the reason `memory_pressure`, which reaches the session as "stopped because the system is running low on memory". That handler reads no threshold of its own — it is a pass-through on Node's event — and it is never registered when this variable is set (bundle 2.1.260, `if (!Ce() && !env.CLAUDE_CODE_DISABLE_BG_SHELL_PRESSURE_REAP)`). | `ansible/roles/setup/claude_code/templates/claude-rc.service.j2:56` | 2026-09-04 |
| --spawn is passed EXPLICITLY and must stay that way. builds a readline interface on stdin to ask which spawn mode to use (it guards that on process.stdin.isTTY). Under systemd there is no stdin to answer, so the host would sit there having never connected — a hang, not an error. ENFORCED by ansible/tests/setup/test_claude_rc_unit.py. | `ansible/roles/setup/claude_code/templates/claude-rc.service.j2:88` | 2026-08-23 |
| MemoryHigh only, deliberately NO MemoryMax. whole cgroup — the host process plus every session it has spawned — so one runaway session would take the OOM kill for all of them, and Restart=always would then bring back an empty host with every phone session gone. MemoryHigh throttles instead of killing. | `ansible/roles/setup/claude_code/templates/claude-rc.service.j2:101` | 2026-08-23 |
| MemorySwapMax is set, and set to a FRACTION of the box's swap rather than to 0. MemoryHigh above throttles the cgroup instead of capping it, and a throttled cgroup's anon pages go to swap — so with swap unbounded the throttle had no ceiling at all. That is how this cgroup took all 8 GiB of the host's swap on 2026-09-05 and starved k3s and kubepods.slice alongside it, while the unit read `active (running)`. Issue #1154. | `ansible/roles/setup/claude_code/templates/claude-rc.service.j2:124` | 2026-09-05 |
| a pod inside the cluster reaches the daemon without Authelia. | `ansible/roles/setup/deploy_ui/CLAUDE.md:26` | 2026-09-10 |
| the Debian revision is a GLOB, never a literal `-1` (#2357). UPSTREAM version -- Renovate strips the revision from the version space it reads (renovate.json), so it cannot propose one, nor prove that `-1` exists for what it proposes. Docker publishes several revisions of one upstream version: the noble/arm64 index carries `containerd.io 2.3.4-1~ubuntu.24.04~noble` and `2.3.4-2~ubuntu.24.04~noble` side by side. A hardcoded `-1` therefore asserted a revision nothing verifies, and a repackage superseding a withdrawn `-1` rendered a spec apt cannot resolve -- `no available installation candidate` from the fresh install and from the deliberate upgrade's pending check. ansible.builtin.apt fnmatches the version in a `name=version` spec (its own `package_best_match` pins the pattern at priority 1001) and hands apt-get the concrete newest match, so apt chooses the revision. Measured on daniel-pi 2026-09-24 in check mode: `containerd.io=2.3.4-*~ubuntu.24.04~noble` built `apt-get --simulate install 'containerd.io=2.3.4-2~ubuntu.24.04~noble'`, the newer revision of the two. Long form in this role's CLAUDE.md. | `ansible/roles/setup/docker_install/defaults/main.yml:35` | 2026-09-24 |
| ` marker at the GOGC line of roles/containers/alloy/templates/docker-compose.yml.j2; the sizing for these two daemons is at `docker_install_gomemlimit_<daemon>` in defaults/main.yml. | `ansible/roles/setup/docker_install/tasks/go-runtime.yml:13` | 2026-09-18 |
| renaming the attachment is safe, and this is the load-bearing correctness claim. subtitle script references a font by its INTERNAL family name (the `Style:` line's fontname), never by the attachment's filename, so libass still matches the font after the header rewrite. mkvpropedit rewrites the header in place without touching or re-encoding any track. | `ansible/roles/setup/fake_remux/files/mkv_attachment_repair.py:9` | 2026-08-29 |
| takes fake_remux_lock, though it shares no ledger with the other two crons. reconciler deletes and swaps the same mkv files this rewrites the header of, and mkvpropedit rewriting a header mid-swap corrupts the file it was called to fix. The lock costs a wait. | `ansible/roles/setup/fake_remux/tasks/main.yml:153` | 2026-08-29 |
| rendered here rather than reusing the k3s role's /etc/rancher/k3s/kuma-push.env. role is host-conditional and tag-scoped, so `initial_setup.yml --tags fake_remux` would render the script without ever writing that file — and the script then exits 0 without pushing, which is silence rather than an error. Same file, same tag, same run. | `ansible/roles/setup/fake_remux/tasks/main.yml:167` | 2026-08-27 |
| rendered by THIS role, not reused from /etc/rancher/k3s/kuma-push.env. host-conditional and tag-scoped (`initial_setup.yml --tags fake_remux`), and the k3s role's env file is written under other tags entirely — so a tag-scoped run of this role would render a script whose env file the same run never wrote. The script then exits 0 without pushing, which is the silent failure this split exists to avoid. | `ansible/roles/setup/fake_remux/templates/fake-remux-push.env.j2:8` | 2026-08-27 |
| ` at that task says why the state directory goes too); | `ansible/roles/setup/gitops_deploy/CLAUDE.md:30` | 2026-09-21 |
| ` in `deploy_git.ci_walk_candidates`. | `ansible/roles/setup/gitops_deploy/CLAUDE.md:112` | 2026-09-21 |
| ` marker above the retirement tasks in `tasks/install.yml`. | `ansible/roles/setup/gitops_deploy/CLAUDE.md:136` | 2026-09-24 |
| ` at the | `ansible/roles/setup/gitops_deploy/CLAUDE.md:162` | 2026-09-21 |
| ` on `deploy_narrow.denylisted_in`). | `ansible/roles/setup/gitops_deploy/CLAUDE.md:164` | 2026-09-21 |
| ` in `deploy_defer.py`'s docstring) — one line per role, first-seen | `ansible/roles/setup/gitops_deploy/CLAUDE.md:197` | 2026-09-24 |
| ` above | `ansible/roles/setup/gitops_deploy/CLAUDE.md:209` | 2026-09-21 |
| ` markers in `deploy_phases.py`. | `ansible/roles/setup/gitops_deploy/CLAUDE.md:242` | 2026-09-21 |
| ` in `deploy_k8s.py` beside that cap. | `ansible/roles/setup/gitops_deploy/CLAUDE.md:253` | 2026-09-21 |
| ` at the | `ansible/roles/setup/gitops_deploy/CLAUDE.md:255` | 2026-09-21 |
| ` at the line that makes it:** the | `ansible/roles/setup/gitops_deploy/CLAUDE.md:261` | 2026-09-21 |
| ` in `deploy_handlers.py` and `test_gitops_deploy_main_branches.py` | `ansible/roles/setup/gitops_deploy/CLAUDE.md:397` | 2026-09-21 |
| ` in | `ansible/roles/setup/gitops_deploy/CLAUDE.md:411` | 2026-09-21 |
| ` in `files/deploy_locks.py`; `deploy.sh` takes the order `deploy_locks.py plan` | `ansible/roles/setup/gitops_deploy/CLAUDE.md:434` | 2026-09-21 |
| queue BEFORE the send, not after. alert_once has already advanced its per-SHA marker by the time we get here — so a process death inside that window (a reboot, a `systemctl stop`, the UPS shutdown chain) used to leave a durable "already alerted" marker with nothing delivered and nothing queued, and the ff-merged channels never re-reach their alert code on a later tick. Queue-first trades lost-on-crash for duplicate-on-crash: a death after the 2xx but before the removal write below makes drain_pending() repost once. At-least-once is the right side for an alert. | `ansible/roles/setup/gitops_deploy/files/deploy_alerts.py:394` | 2026-09-05 |
| this alert is a one-shot detection, not the durable signal. origin SHA (alert_once) and the ff-merge below clears `behind_since` -- the deployer's own "still behind" marker -- so every other monitored marker reads clean while the cluster keeps running the old manifests (issue #947). The durable signal is a daniel-box cron reading `probe.py releases --stale-only` against the release records `roles/k8s/manifests/tasks/release_stamp.yml` writes on every real apply -- see this role's CLAUDE.md, "k8s-platform roles are auto-deployed ONLY for an image-pin bump...". | `ansible/roles/setup/gitops_deploy/files/deploy_alerts.py:558` | 2026-09-05 |
| ` measured the cost of. | `ansible/roles/setup/gitops_deploy/files/deploy_broad_k8s.py:82` | 2026-09-24 |
| ` in `deploy_handlers.handle_broad`'s own failure arm gives (#2459). | `ansible/roles/setup/gitops_deploy/files/deploy_broad_k8s.py:232` | 2026-09-24 |
| roles/setup/gitops_deploy/ — the deployer's own role — is NOT here, and applies itself like any other setup role. It sat here until 2026-09-01 on the claim that applying it "runs a playbook whose handler restarts the unit executing the tick", so the run would be SIGTERMed partway. The handler does not restart anything: `Run gitops-deploy once` is `ansible.builtin.systemd: state: started`, and Ansible's systemd module treats an `activating` unit as already running (`is_running_service` accepts `active` and `activating`), so from inside a tick it is a no-op. The only other handler is a daemon-reload, which a running oneshot unit survives. What the park DID do, three times on 2026-09-01 alone (#707, #712, #714): stop every other session's landing until an operator hand-ran `initial_setup.yml --tags gitops_deploy` in the primary checkout and ff-merged, because `deploy.sh` refuses a tree behind origin and the tick would not move it. A self-apply that fails takes the broad-apply failure path — hold_sha, hold_plane, alert — which is the same containment every other setup role gets, and the code it installs has passed master CI, which is the same gate every other role gets. The unit's own state (`config.env`, `/opt/gitops-deploy/*.py`) is read at the START of a tick and a mid-tick overwrite reaches only the next one, which is the tick that should run the new code anyway. | `ansible/roles/setup/gitops_deploy/files/deploy_changes.py:168` | 2026-09-01 |
| an unapplyable setup ROLE no longer parks the tick. | `ansible/roles/setup/gitops_deploy/files/deploy_defer.py:16` | 2026-09-11 |
| the gate chooses the NEWEST GREEN ANCESTOR, the walk is bounded, and a red | `ansible/roles/setup/gitops_deploy/files/deploy_git.py:151` | 2026-09-11 |
| a bring-up playbook (and a setup path naming no role) still parks; a setup ROLE this deployer cannot apply no longer does — it fast-forwards and leaves the durable `manual_plane` marker instead. Parking held every other session's landing behind work only a hand could do. `deploy_defer`'s module docstring carries the measurement. | `ansible/roles/setup/gitops_deploy/files/deploy_handlers.py:130` | 2026-09-11 |
| the secrets page rides the NON-CONTENTION exits — this arm, and the two in `apply_broad_k8s` — rather than firing once before the loop (#2459). #2383 put it before the loop so no failure arm could drop it, and that also sent it on the contention arm, which resets the tree seconds later. Its text says the range was fast-forwarded and names `ansible-playbook` as the remedy, and both are false after a reset: the operator whose `deploy.sh` holds the lock would have redeployed the OLD secret from a tree back on `local`, and the dedupe marker then suppressed the page the re-merging tick owes. Sending it from each exit that leaves the range merged keeps #2383's property — every one of them is reached with `local == origin`, where no later tick re-evaluates. | `ansible/roles/setup/gitops_deploy/files/deploy_handlers.py:215` | 2026-09-24 |
| consult the gate BEFORE the ff-merge, never after. to STAGING_GATE_TIMEOUT_S + STAGING_EXPECT_TIMEOUT_S, and a process death inside that window used to leave local == origin with nothing deployed — next_action() then returns noop forever (the SHA is already merged, so nothing re-triggers), `last_run` keeps ticking, and both Kuma tiles stay green over a permanently stranded deploy. Merging after the gate makes the same death self-healing: local is still behind, so the next tick re-evaluates. Whether the verdict blocks is staging_blocks' decision; while config.staging_gate_blocking is false it never does, and this branch is the slice-3 behaviour unchanged. | `ansible/roles/setup/gitops_deploy/files/deploy_handlers.py:275` | 2026-09-05 |
| `origin[:8]` is a fixed slice while volume-snapshot names with `--short=8`, a MINIMUM width. They diverge only when 8 chars collide, and then the prefix misses by one character and volume-revert's no-snapshot assert fires before the scale-down — the safe failure. Measured zero ambiguous 8-char prefixes across ~39k objects. Full analysis in this role's CLAUDE.md; two reviewers re-derived it on 2026-08-22, hence the marker. | `ansible/roles/setup/gitops_deploy/files/deploy_handlers.py:366` | 2026-09-05 |
| the batch-abort blast radius is accepted, and this SECOND cap on claim-declaring services is what bounds it (2026-08-22 review H2). ansible/tasks/k8s_batch.yml joins the batch into one playbook run with no `rescue`, on the rollback path too, so one service's revert failing during a rollback aborts every co-batched service's rollback and leaves those already reset mid-revert; the fix that would close it does not fit the unit's ceiling (the arithmetic below). Long form: docs/gitops-pipeline.md, *The deployer's record*. The rollback budget K8S_ROLLBACK_TIMEOUT_S is derived for the worst SINGLE promoted service that declares k8s_autodeploy_snapshot_pvcs — but deploy_k8s joins the whole batch into one playbook run, and each such service pays its own snapshot + revert phase serially inside it (only the rollout WAIT is deduped, by k8s/rollout-drain). Measured from role sources: one radarr/sonarr-shaped service is ~1200s against a 1320s budget, two co-batched ~1680s, three ~2160s. Past the budget `run()`'s killpg fires MID-REVERT — after volume-revert has scaled the workload to zero replicas and attached the volume with disableFrontend: true — stranding a service at zero replicas with its volume in Longhorn maintenance mode. | `ansible/roles/setup/gitops_deploy/files/deploy_k8s.py:287` | 2026-09-21 |
| the lock order is `server-deploy-all.lock` first -- shared when the run names services, exclusive when it names none -- then each service's own lock in sorted order. Sorted order is what makes two overlapping scoped runs deadlock-free; taking `all` before any service is what makes a full run and a scoped run deadlock-free. The deployer takes the TREE lock (systemd's ExecStart `flock`) and then these, holding both across the playbook. `scripts/deploy.sh` takes the tree lock, snapshots, RELEASES it, and only then takes these -- and never re-takes the tree lock -- so the two orders cannot form a cycle. (ADR-0017) | `ansible/roles/setup/gitops_deploy/files/deploy_locks.py:42` | 2026-09-12 |
| a full run on any doubt. the play itself reads, a tag list covering most of the fleet, a crash here — lands on this branch and runs what the tick ran before. `except Exception` is deliberate and the narrowest correct width: the call decodes a subprocess's output, so it can raise UnicodeDecodeError as well as SubprocessError, and an escape parks every landing behind this tick — `plan` runs BEFORE the ff-merge. A missed consumer is a service left silently stale until something unrelated redeploys it, and nothing reports that; a full run is only slow. The rules and what each one refuses are in scripts/deploy_tools/narrow_broad.py. | `ansible/roles/setup/gitops_deploy/files/deploy_narrow.py:206` | 2026-09-11 |
| the broad plane ignores `K8S_AUTODEPLOY_DENYLIST` (issue #1962). PROMOTION into the k8s auto-deploy machinery — `split_k8s_auto_deploy` — whose pre-apply snapshot, staging gate and automatic rollback are what a role declares `k8s_autodeploy: false` to stay out of: a probe-less workload the rollout gate cannot see, migrating state a revert would corrupt, a platform role whose rollback needs the access it just broke. The broad plane has none of that machinery. It runs the plain playbook forward-only and holds the SHA on failure, which is what an operator's `deploy.sh --tags <role>` does for the same role, and it has applied every denied role that way on every unscoped run since 2026-08-29. Three things made a filter the wrong shape. It could only gate the NARROWED path: a refused range still runs the whole play, over all forty denied roles, so the filter would gate the derivation that is certain and leave the one that is not wide open. Forty of the fifty-four k8s roles are denied, so a filtered narrowed range would mostly apply nothing and hand a tag list to a human — for a change a human authored and merged, with `land.sh` already waiting on this tick to apply it. The third reason, that a dropped tag would be a service left silently stale with nothing to name it, no longer holds on its own: since #1993 `Release Staleness Drift` asks `narrow_broad` the same per-path question this plan asks, from each service's release record to origin/master, so a denied role whose render reads a merged inventory key or macro reads STALE until it is re-stamped (`_deploy_plane_stale` in scripts/diagnostics/probe_lib/releases.py). That makes a filter viable, not wanted: the first two reasons stand, and `manual_plane` is still rendered as a setup role by every one of its five readers, so a deferral here would still have no k8s-shaped marker. What the denylist still governs on this plane is who MERGES: renovate.json's `manual — k8s_autodeploy: false` rule names `group_vars/all.yml` beside a denied role's own defaults (#1936), so no pin a denied role reads through a shared key merges unattended. `denylisted_in` exists so the journal line above can name which of a narrowed apply's tags were denied ones; the `broad_applied` marker carries the same tag list durably, and the denied subset of it is that list intersected with the denylist. | `ansible/roles/setup/gitops_deploy/files/deploy_narrow.py:235` | 2026-09-18 |
| the gate is the FILE-level flag (`k8s_autodeploy_enabled_in_file`), never the post-disarm `k8s_autodeploy_enabled`. `gitops_deploy.py` flips the latter to False when the rendered denylist is empty — fail-closed, so a truncated config.env cannot widen what auto-deploys. Gating here on the flipped value made that one state unhealable: a config.env that LOST its denylist line disarmed the very reconcile whose re-render is the repair, and the host stayed that way until an operator ran `initial_setup.yml --tags gitops_deploy` (issue #1317). Reading the file-level flag keeps the three states apart — the file says off, so skip; the file says on with no denylist, so re-render; the file says on with a denylist, so compare. A host that legitimately has auto-deploy off still renders nothing, on any tick, because its file flag is false. | `ansible/roles/setup/gitops_deploy/files/deploy_phases.py:300` | 2026-09-06 |
| an EMPTY `declared` is damage, never evidence — guard it before the comparison, unconditionally, rather than only when the config denylist is empty too. A `git ls-tree` that lists no role under roles/k8s/ reads as `frozenset()`, which against a config whose denylist line was lost — also `frozenset()` — compared EQUAL and marked the damaged config reconciled for that checkout, so the repair never ran (issue #1331, reachable since #1321 moved this gate to the file-level flag). The authoritative derivation takes the same stance for the same reason: `filter_plugins/k8s_autodeploy.py:156-160` raises rather than render an empty denylist, because an empty result means the derivation is broken. No marker write, as in the unreadable-ref branch above: an empty listing is one `git ls-tree` and zero `git show` reads, so retrying every tick is cheap — do not "fix" the log line by claiming the SHA, which is exactly the bug. The log wording differs from the branch above so an operator can tell an empty listing from a git failure. | `ansible/roles/setup/gitops_deploy/files/deploy_phases.py:325` | 2026-09-06 |
| a failed self-render does NOT write hold_sha, unlike `handle_broad`'s failed apply. The two failures contain differently. A half-applied broad plane leaves live state nothing recorded, so parking is the containment. This render only rewrites the deployer's own config.env; a failure leaves the OLD file, which is the state the tick already tolerates — auto-deploy stays disarmed by the origin comparison, which is the fail-safe direction. Parking every unrelated service deploy behind a config render would be a strictly larger outage than the one this heals. | `ansible/roles/setup/gitops_deploy/files/deploy_phases.py:364` | 2026-09-06 |
| the ceiling has since moved and the arm stays forward-only anyway. | `ansible/roles/setup/gitops_deploy/files/deploy_remediation.py:50` | 2026-09-01 |
| name a full deploy for the shared roles instead of routing them to `cs.broad`. | `ansible/roles/setup/gitops_deploy/files/deploy_remediation.py:332` | 2026-09-01 |
| subtract-and-keep, with no guard on the origin SHA the command was printed for. A second range that needs a tag ALREADY in the row (PR-B also needs `kubeconfig`) leaves the row unchanged, so an operator who applied `kubeconfig` before PR-B fast-forwarded clears PR-B's pending work with their own. Closing that needs the printed command to carry a token of what the row was, which widens the marker grammar the sidecar exists to leave alone. Accepted in #2349 (PR #2364). | `ansible/roles/setup/gitops_deploy/files/deploy_state.py:325` | 2026-09-24 |
| falls back to the role default 3, NOT to 0 — same argument as the claim cap below, which this line did not carry until 2026-08-24. 0 means UNCAPPED, so a config.env that lost this key would restore the unbounded batch on exactly the host whose config is damaged. The live case is truncation, not age: in templates/config.env.j2 the denylist is line 23 and this key is line 27, so a half-written file keeps a matching denylist plus ENABLED=true and drops only the cap — passing the fail-closed denylist guard below while uncapped. | `ansible/roles/setup/gitops_deploy/files/gitops_deploy.py:117` | 2026-08-24 |
| exit 0, no Discord post, no last_run. host whose inventory has already retired the deployer, so a non-zero exit would page through OnFailure every ten minutes from a webhook that host should no longer hold, and a last_run write would stamp liveness onto state nothing reads. The journal line is the signal; the fix is the role's teardown, not a louder tick. A spurious refusal on the real deployer is not silent either: last_run stops advancing and GitOps-Alive goes stale inside GITOPS_MAX_AGE_MIN (90 minutes), which is the backstop this exit 0 leans on. | `ansible/roles/setup/gitops_deploy/files/gitops_deploy.py:397` | 2026-09-11 |
| retire the staging-backfill ratchet rather than keep it as the gate's exerciser. The operator chose this on 2026-09-24 (#2414), after reading the case for keeping it (closed PR #2457). The cost is real. The hourly ratchet was the only thing that drove the staging gate regularly, and a real gated tick reaches `consult_staging` about once a month. A gate that rots answers NO_VERDICT, and `staging_blocks` blocks only on REJECTED, so a rotted gate lets prod through without blocking anything. What remains to notice that is `consult_staging`'s own Discord alert on every non-PASS of a real tick. The ratchet did not alarm on a NO_VERDICT either (#2455), so the exercise it provided had no alarm attached. The long form is docs/staging-phase-c.md, *After the flip: the ratchet is retired*. | `ansible/roles/setup/gitops_deploy/tasks/install.yml:133` | 2026-09-24 |
| /var/lib/gitops-deploy is reaped, not kept. host's checkout — hold_sha, behind_since, the per-SHA alert throttles, the undelivered alert queue — and a host that is not the deployer has nothing that reads it: monitor-bridge hostPath-mounts daniel-box's copy alone (bridge/config_service.py, GITOPS_STATE_DIR). Kept, it would hand a re-enabled deployer a hold and a behind-origin clock from before it was retired; reaped, the first tick writes a fresh, honest state. The forensic value it had for #1733 was read and recorded on that issue before this task existed. | `ansible/roles/setup/gitops_deploy/tasks/teardown.yml:96` | 2026-09-11 |
| this script RECONCILES, where its sibling github-ruleset-drift.sh only alerts. ruleset is a setting a human changes on purpose, and reconciling it would undo that change with no signal. The limit is not: the only way it changes without a human is by expiring, and re-applying an expired setting is restoring the declared state, not overriding a decision. A human who wants it off changes `gitops_deploy_interaction_limit` in defaults/main.yml — this script applies whatever that declares, and `none` disarms it by way of a DELETE. | `ansible/roles/setup/gitops_deploy/templates/github-interaction-limit.sh.j2:15` | 2026-09-06 |
| contention exits 75 and the unit SUCCEEDS — do not "fix" this back to a failure, and do not raise -w to compensate. Until 2026-08-23 a timeout exited 1, so every long operator `./scripts/deploy.sh` (~20min, holding this same lock for its whole run back then; it now holds it only for a snapshot of HEAD) paged via OnFailure: seven such silent exit-1 ticks in the seven days to 2026-08-23, each logging nothing at all because flock died before the deployer started. Raising -w cannot fix it — this unit's own worst case is STAGING_GATE_TIMEOUT_S + STAGING_EXPECT_TIMEOUT_S + K8S_DEPLOY_TIMEOUT_S + K8S_ROLLBACK_TIMEOUT_S = 3240s against TimeoutStartSec 3600s, so the largest legal -w is 360s, still short of a routine 20-minute deploy; test_gitops_discord_contract.py::test_k8s_deploy_ timeout_budget_survives_max_flock_contention asserts that bound and goes red if you try. So -E 75 + SuccessExitStatus=75 instead, the same "lock busy, nothing deployed" contract scripts/deploy.sh:819 already uses. Contention is not silent starvation: the flock path never writes last_run, so contention lasting past GITOPS_MAX_AGE_S (90min, check.py:148) still pages via GitOps-Alive. (ADR-0011) | `ansible/roles/setup/gitops_deploy/templates/gitops-deploy.service.j2:96` | 2026-08-23 |
| ` measured. | `ansible/roles/setup/gitops_deploy/tests/test_gitops_deploy_broad_k8s.py:114` | 2026-09-24 |
| ` marker says a crash here lands on the fallback, so prove it can. | `ansible/roles/setup/gitops_deploy/tests/test_gitops_deploy_broad_narrow.py:149` | 2026-09-11 |
| ` on `deploy_narrow.denylisted_in` is the reasoning; this pins the | `ansible/roles/setup/gitops_deploy/tests/test_gitops_deploy_broad_narrow.py:184` | 2026-09-18 |
| the broad plane ignores `K8S_AUTODEPLOY_DENYLIST`" in text | `ansible/roles/setup/gitops_deploy/tests/test_gitops_deploy_broad_narrow.py:217` | 2026-09-18 |
| 192.168.140.0/24, chosen against a census of what daniel-server actually routes rather than by picking an unused-looking number (2026-08-27). In use there: 10.0.0.0/24 (LAN), 10.42.0.0/16 and 10.43.0.0/16 (k3s pods and services), 10.200.0.0/16 and 172.17.0.0/16 (bridges left by the retired Docker install), and 192.168.122.0/24 (libvirt's own `default`, defined-but-stopped). Sitting outside 10/8 entirely means a future k3s or Docker range cannot grow into it. ENFORCED by ansible/tests/staging/test_staging_network.py, which re-checks the overlap. | `ansible/roles/setup/hypervisor/defaults/main.yml:31` | 2026-08-27 |
| pin the checksum even though `current` rotates (2026-08-27). roughly monthly, so a host that has not downloaded the image yet WILL eventually fail here — which is the intended failure. A staging substrate silently built from a different image than the one reviewed defeats the point of having a reviewed substrate. Re-pin from https://cloud-images.ubuntu.com/noble/current/SHA256SUMS when it fails. | `ansible/roles/setup/hypervisor/defaults/main.yml:61` | 2026-08-27 |
| transient domain (`virsh create`), never `virsh define`. guest is DEFINED and nothing in this role undefines one, so a second persistent domain would silently break hypervisor teardown. A transient domain vanishes when it is destroyed, and its disk is recreated from the base image on every run and deleted on every exit, pass or fail. That is the answer to the issue's cleanup constraint: no restored etcd database, token copy or R2 credential outlives the run anywhere. What persists is under hypervisor_etcd_drill_log_dir — the drill's stdout plus k3s's restore.log and server.log, none of which carry a credential — pruned at hypervisor_etcd_drill_log_retention_days. | `ansible/roles/setup/hypervisor/defaults/main.yml:135` | 2026-09-11 |
| the packaged `default` NAT network is stopped and un-autostarted rather than left alone. Installing libvirt otherwise brings up virbr0 and a set of NAT/forward rules on a host that already runs k3s's flannel and kube-router chains — an undeclared third writer of firewall state, appearing as a side effect of a package install. The staging network is declared explicitly in its own slice; this makes sure nothing else is. | `ansible/roles/setup/hypervisor/tasks/install.yml:62` | 2026-08-27 |
| the rendered XML pins a UUID, the live one where the network exists and a deterministic UUIDv5 over the name on a fresh host. `net-define` replaces an existing network only when the UUIDs match; handed XML with no <uuid> it mints a fresh one and refuses on the name collision ("network 'staging' already exists with uuid ..."), which is how the first template change since bring-up failed the apply on daniel-server on 2026-09-11 (#1175). The network on that host predates this pin and carries a random UUID, and libvirt refuses a same-name define under any other UUID even after net-undefine (the active transient network still collides), so the live UUID is the only value that keeps daniel-stage's bridge up through the change. The nwfilter template pins the same way. | `ansible/roles/setup/hypervisor/tasks/network.yml:71` | 2026-09-11 |
| a libvirt nwfilter, not a ufw `route deny` and not a FORWARD hook script. | `ansible/roles/setup/hypervisor/templates/staging-nwfilter.xml.j2:6` | 2026-08-28 |
| drop one destination, do not allowlist. | `ansible/roles/setup/hypervisor/templates/staging-nwfilter.xml.j2:21` | 2026-08-28 |
| blanket, with no exception for the in-cluster registry. | `ansible/roles/setup/hypervisor/templates/staging-nwfilter.xml.j2:27` | 2026-08-28 |
| the UUID is pinned, derived from the filter's own name. | `ansible/roles/setup/hypervisor/templates/staging-nwfilter.xml.j2:48` | 2026-08-28 |
| the cluster's pod and Service ranges are fenced alongside the LAN, and the | `ansible/roles/setup/hypervisor/templates/staging-nwfilter.xml.j2:66` | 2026-08-28 |
| `always`, not `crons` — this is a dependency of tasks in OTHER roles under other tags, so any tag that selects a consumer must also select this. optimize_pi's Pi crons are tagged `sd-health` / `recovery-health`, so the one-liner that role's CLAUDE.md documents (`initial_setup.yml --tags recovery-health -e target=daniel-pi`) rendered a cron that sources a file the same run never copied. Enumerating consumer tags here would re-break the next time a cron picks up a new tag; `always` cannot drift. Cost is one idempotent copy per tag-scoped run of initial_setup.yml. | `ansible/roles/setup/initial_setup/tasks/crons.yml:7` | 2026-08-23 |
| 0700 owned by sys_user, not 0755 root — embeds a live Kuma push token, and the cron below runs as sys_user, so root:root 0700 would lock out its own caller. Same marker as roles/k8s/configarr/tasks/main.yml; test_secret_bearing_host_scripts_are_not_world_ readable.py holds every such script to it. | `ansible/roles/setup/initial_setup/tasks/crons.yml:646` | 2026-09-17 |
| 0700 owned by sys_user, not 0755 root — embeds two Kuma push tokens, and the cron below runs as sys_user. See the docs-refresh marker above. | `ansible/roles/setup/initial_setup/tasks/crons.yml:718` | 2026-09-17 |
| the engine packages are held, so a dist-upgrade never touches them. this task's dist-upgrade replaced containerd.io, docker-ce and the plugins on daniel-pi with every container running: autoheal's shim died (`failed to create TTRPC connection`), ssh refused connections for ~20 min, and docker-proxy sat unhealthy behind a stale socket bind-mount until a hand redeploy recreated it (#1961). live-restore keeps containers up across a dockerd restart; it does nothing for a shim binary swapped under them. The cost is that a Docker security fix waits for `--tags docker-engine-upgrade`, which stops the containers, upgrades, re-holds and recreates them -- see docker_install's CLAUDE.md. Guarded on the binary because dpkg_selections fails on a name dpkg has never seen: a fresh host passes through here and docker_install holds them the moment it installs them. | `ansible/roles/setup/initial_setup/tasks/host-basics.yml:90` | 2026-09-18 |
| the LAN is exempt from SSH rate-limiting, and this rule must sort ABOVE the `limit` rule below. ufw matches in order and takes the first hit, so an allow appended after the limit rule is inert — the "present but does nothing" shape that #508 already cost this role once, described at length further down. | `ansible/roles/setup/initial_setup/tasks/network.yml:85` | 2026-08-29 |
| keep `enabled`. 2026-09-11 on a fire forced with the `-e` override below, 73 snapshots across 44 volumes (50 GB of replica data per node): the run took 6m21s and read ~24 GB per node at 40-57 MB/s. The hashing CPU is instance-manager's own, 0.21 → 0.37-0.50 cores over the window; node-level CPU is not attributable, because daniel-box swings 1.6-2.4 cores across any 7-minute window. instance-manager's working set did not move (1.44 GB before, during and after), MemAvailable never dropped below 17.5 GB, and no Longhorn pod restarted — the memory pressure #1243 warns about did not appear. Six minutes every seventh day at 00:00 UTC is acceptable. The numbers and the queries that produced them are on #1364. | `ansible/roles/setup/k3s/defaults/main.yml:294` | 2026-09-11 |
| 4 GiB, raised from 2 GiB on 2026-08-24. NOTHING today — the largest candidate is valheim-config at ~1.3 GB", and both halves had gone wrong: valheim-config was 2.83 GiB of actualSize and HAD been excluded (25 volumes declared backed-up, 24 in the candidate file), and the 1.3 GB figure was its B2-STORED size while this cap is applied to actualSize — the wrong quantity was compared when the cap was first set. Compare `actualSize` from the Volume CR, never the stored-block total from `probe.py b2-longhorn`. Cost of the raise is ~100 Class B GETs and ~1.34 GB egress once per 25-night rotation. | `ansible/roles/setup/k3s/defaults/main.yml:418` | 2026-08-24 |
| 2700s, raised from 1800s with the cap on 2026-08-24 — and this is NOT free to raise further. The 04:10 cron is placed to clear the 05:15 manifest-prune window, which would otherwise see the drill's transient restore-drill-* objects, so the timeout ceiling is 05:15 minus 04:10 minus a margin. 2700s ends by 04:55. A larger cap therefore cannot simply buy more time here: past ~3900s the two windows collide and the cron must move first. | `ansible/roles/setup/k3s/defaults/main.yml:459` | 2026-08-24 |
| hourly, not the */10 + 1200s pairing every sibling heartbeat above uses — the horizon is measured in DAYS, so two runs in the same hour cannot find different answers, and a */10 find over the home tree is 6x the IO for no extra signal. The paired Kuma interval must then exceed the PRODUCER'S PERIOD, which is 3600s plus the run: static-monitors.yaml.j2 carries 5400s, leaving 30 min of margin. Move both or neither — a 1200s interval against an hourly cron fires DOWN every hour with nothing wrong (the failure the Cloudflare DDNS entry records). | `ansible/roles/setup/k3s/defaults/main.yml:880` | 2026-08-24 |
| ` at that branch of `deploy_alerts.alert_deferred` in roles/setup/gitops_deploy/files/. | `ansible/roles/setup/k3s/defaults/main.yml:905` | 2026-09-17 |
| `--list-only`, weekly, and NOT the full restore. end to end: credentials, bucket, folder, download, decompression. The FULL drill does not pass on daniel-box at all — five structural failures documented at scripts/backup/etcd_restore_drill.sh:43-64 — and scheduling it would be strictly harmful: cleanup() sets KEEP=1 on ANY non-zero exit and scratch dirs are swept only at +30 days, so a monthly failing full drill holds one to two restored etcd databases plus cluster-token copies in /var/tmp as a steady state. The scheduled list-only arm has no such cost: it exits at the drill's `--list-only` branch, BEFORE the `install -d "$SCRATCH"` that creates the directory at all, so however often it fails it writes nothing to /var/tmp. That asymmetry is why one mode is scheduled and the other is not ON THIS HOST. The full drill IS scheduled since #1175 — monthly, in a throwaway guest on daniel-server (`etcd_drill_full_cron` in group_vars, mechanism in roles/setup/hypervisor), where the guest's disk is deleted after every run and so nothing accumulates anywhere. | `ansible/roles/setup/k3s/defaults/main.yml:962` | 2026-08-28 |
| `when:` on the existing `import_tasks`, not a switch to `include_tasks`. is static, so each task keeps its own tags and `--tags longhorn-backup` still selects; a dynamic include would hide those tags from selection and break the tag-based deploys. | `ansible/roles/setup/k3s/defaults/main.yml:1362` | 2026-08-27 |
| a kubectl fetch that fails is a DOWN with a reason, not an empty result. this was ported from sent every one of these reads to `2>/dev/null` (`git show 62c488dd^:ansible/roles/setup/k3s/templates/longhorn-backup-health.sh.j2`, lines 144, 176, 218, 344, 395, 412 and 443) and let the empty capture flow into the checks; the first Python port kept that shape as `... if rc == 0 else []`. Both read an API-server timeout, a 500 or RBAC drift as "nothing errored, nothing is stale, this tier has no volumes" — a green light over data nobody looked at, on the deadman for the Longhorn backup plane. Parity with the bash was the wrong target here, so this departs from it: a reader that cannot see the cluster says so. Issue #1061 has the per-line analysis. | `ansible/roles/setup/k3s/files/longhorn_backup_health.py:211` | 2026-09-04 |
| `copy`, not release_bin.yml: these are operator-invoked (deliberately unscheduled — see the templates above), not cron targets, so there is no rollback-unit reason to version them alongside the backup-health release, and a fixed /opt path matches configarr-health's shape (roles/k8s/configarr/tasks/main.yml), which this install mirrors. Consequence an operator should know: the two `.sh.j2` shims above deploy as a VERSIONED release (release_bin_group: k3s-backup-health below) while this Python deploys as a plain copy, so a release rollback of that group reverts the shims and leaves the Python at whatever SHA last deployed here — the two can drift apart. Accepted because both scripts are dry-run-by-default and DELIBERATELY NOT SCHEDULED (defaults/main.yml's k3s_render_stamp_groups comment): an operator invoking either by hand is already reading its dry-run output before ever passing --apply, which is where a version mismatch between shim and classifier would show up. | `ansible/roles/setup/k3s/tasks/health-crons.yml:99` | 2026-09-04 |
| the fix for a sub-tag that renders without stamping is to DROP the sub-tag, never to add it here. `--tags restore-drill` used to render 1 of this group's 5 templates and stamp none, which reads as drift on 1 script — a false RED. Adding `restore-drill` to this task's tags restamps all 5, declaring the 4 it never rendered to be current: a false GREEN, and the exact defect the 2026-08-23b review's M6 was raised for. A false RED costs an investigation; a false GREEN costs the guarantee. Two reviewers proposed the additive fix on 2026-08-24 and both were wrong; the sub-tag is gone entirely — the deploy task above is now a single loop over all 5 backup-health scripts (including the restore drill), so there is no per-script tag left to add one to. Contradict this only with a stamp mechanism that can record a PARTIAL group. | `ansible/roles/setup/k3s/tasks/health-crons.yml:589` | 2026-08-24 |
| the wildcard, rather than the node's own routable address. interpolated `server_ip` until 2026-09-10, and a bind to a specific address depends on that address already being assigned when CoreDNS starts. On the 2026-09-09 boot it was not: CoreDNS came up, served DNS on `bind 127.0.0.1` above, bound no metrics listener, and systemd reported the unit healthy with NRestarts=0 throughout — `ss -lnt` counted zero sockets on the metrics port for the rest of the day, and the only thing that went red was a Prometheus target several layers away, naming a connection refusal rather than a bind failure (GitHub issue #1476). A wildcard bind cannot fail that way, because it depends on no address being assigned first. The exposure is unchanged: UFW's INPUT is default-deny and node.yml opens this port only to the pod CIDR and the agent nodes. | `ansible/roles/setup/k3s/templates/host-corefile.j2:33` | 2026-09-10 |
| the candidate list is published from the FULL eligible set, and the pin is applied only after it. Filtering in the jq above — the shape until 2026-09-21 — wrote a one-line candidates file on a pinned run, and check 8 sizes its coverage window from that count and iterates only those names: one operator drill shrank the window from ~25 days to ~1 and dropped every other volume out of the coverage check until the next nightly run rewrote it. | `ansible/roles/setup/k3s/templates/longhorn-restore-drill.sh.j2:126` | 2026-09-21 |
| the waiver is gated on the SOURCE volume's actualSize as well as on the name (#2393). The name alone waived both content assertions whatever the volume held, and nothing else in this drill compares the restored content against the source — so the day n8n-files starts holding data, an empty restore would still have stamped success and check 8 would still have read it as restore-proven. The defaults comment claimed removing a volume from the list was optional for the drill to stay honest; it was not, and both are corrected together. | `ansible/roles/setup/k3s/templates/longhorn-restore-drill.sh.j2:341` | 2026-09-24 |
| an operator-pinned run leaves the global stamp alone. NIGHTLY drill still running"; a hand run refreshing it would hide a dead cron behind the one volume someone drilled by hand — the exact failure the drill exists to make visible. The per-volume stamp below is still written: that one answers "does this volume restore", which a hand run proves just as well. | `ansible/roles/setup/k3s/templates/longhorn-restore-drill.sh.j2:379` | 2026-09-21 |
| `k8s_primary_node`, not the literal `daniel-box` it read until 2026-08-27. workload pins this must stay in step with are all that variable already, and a literal here was the last way the two halves could drift apart. It also lets a single-node staging cluster announce from its own node instead of naming prod's. | `ansible/roles/setup/k3s/templates/metallb-pool.yaml.j2:64` | 2026-08-27 |
| no `globalDefault: false` on any of these, though it read that way until 2026-08-27. It is the default, so stating it changes nothing about scheduling — and stating it made every `kubectl apply` of this file report "configured" forever. The API server drops globalDefault when it is false (an omitempty zero value), so the field is present in the manifest and in kubectl's last-applied annotation but absent from the live object. The three-way merge therefore finds a patch to make on every single run, and never converges. | `ansible/roles/setup/k3s/templates/priorityclass.yaml.j2:1` | 2026-08-27 |
| ` at the `cs.k8s` branch of `deploy_alerts.alert_deferred` (roles/setup/gitops_deploy/files/), the `#947` bullet in roles/setup/gitops_deploy/CLAUDE.md for the full account, and this role's CLAUDE.md for where this check sits among the other drift crons. | `ansible/roles/setup/k3s/templates/release-staleness-check.sh.j2:9` | 2026-09-03 |
| 0700 root:root, not 0755 — the cron below is its only caller and runs as root. The script renders NO credential: it reads the upsmon endpoint out of /etc/nut/upsmon.conf at runtime with a field-scoped awk, and names nut_monitor_password only in a comment saying why that read is field-scoped. The 0700 is still ENFORCED, because scripts/secrets_mgmt/secret_bearing_host_paths.py matches that comment mention and its census errs toward denying (a false deny costs a prompt, a false permit costs a token) — test_secret_bearing_host_paths.py pins this path for exactly that reason (#2041). | `ansible/roles/setup/nut_host/tasks/main.yml:120` | 2026-09-18 |
| FINALDELAY stays 5 on every host, and is deliberately NOT parameterised to stagger the two poweroffs. The tempting design is to let daniel-box shut down before daniel-server, whose pod hosts upsd, so it cannot be stranded when upsd disappears. It is unnecessary: a secondary needs upsd only to OBSERVE FSD, not to complete SHUTDOWNCMD, and both hosts observe it within one POLLFREQALERT (5 s) of the pod's `upsmon -c fsd` — long before daniel-server's poweroff reaches containerd. Staggering would also rest on FINALDELAY applying in secondary mode, which upsmon.conf(5) documents for primary mode only. Full reasoning in roles/k8s/nut/CLAUDE.md. | `ansible/roles/setup/nut_host/templates/host-upsmon.conf.j2:30` | 2026-08-28 |
| STATUS stays `down` even when the restart worked. successful recovery would make an auto-restart loop invisible: a container crashing every 5 minutes would read green forever, which is the failure mode that makes self-healing worse than no healing. One DOWN beat per cycle that needed intervention keeps the episode in Kuma and in the alert history, and the next clean cycle clears it. Re-proposed by #1784 as "an UP with a warning would read more honestly", after two single-cycle `not running: autoheal; restarted: autoheal` episodes on 2026-09-02/03, and declined 2026-09-11: those two episodes ARE the loop this rule exists to keep visible, and no evidence was offered against the original reasoning. The recurring fault worth fixing is that autoheal stops at all, filed separately. | `ansible/roles/setup/optimize_pi/templates/pi-recovery-health.sh.j2:130` | 2026-09-11 |
| no verdict reads as NOT contained. | `ansible/roles/setup/renovate_agent/files/renovate_agent.py:216` | 2026-09-18 |

## roles/containers

| Marker | File | Decided |
|---|---|---|
| ` marker on `ports:` in the compose template). | `ansible/roles/containers/alloy/CLAUDE.md:21` | 2026-09-17 |
| ` marker at the `GOGC` line says what that costs on this host (the heap is | `ansible/roles/containers/alloy/CLAUDE.md:61` | 2026-09-18 |
| GOGC=off under GOMEMLIMIT, to cut how often this heap is faulted back in, and the faults that remain are accepted (#1967). This host swaps by design (`vm.swappiness=130` to zram, roles/setup/optimize_pi), global reclaim swaps this heap out between cycles, and the mark phase faults it back in. Measured 2026-09-18 at GOGC=50: 28,423 major faults on this cgroup in 300 s (95/s), a third of the host's 84,938, 0.52 cycles a minute — 10,800 faults a cycle against a 9,500-page heap, so by the next cycle the whole heap is cold. GOGC=25 the same day, after a 16-minute warm-up: 1.0 cycles a minute, 33,667 in 300 s of the host's 100,479 — the same 33.5% share, at the same host pressure (memory PSI `full avg300` 4.0-4.6%), so no reduction; 6,700 faults a cycle, half the heap. The rate is cycles × the part of the heap that went cold, and below one cycle every ~2 minutes that part is all of it, so fewer cycles means proportionally fewer faults. | `ansible/roles/containers/alloy/templates/docker-compose.yml.j2:116` | 2026-09-18 |
| the port stays published on the LAN and the surface behind it is narrowed instead. Two consumers reach it from off-box and neither can use a loopback or a bridge address: claude-otel's `alloy-pi` Prometheus job scrapes k8s_pi_client_ip:12345 as a STATIC target (roles/k8s/claude-otel/templates/prometheus.yaml.j2), and monitor-bridge's detached-container arm expects `alloy` to report a `->` mapping (PI_PUBLISHED_PORTS, pinned by ansible/tests/services/test_pi_publishing_containers.py). Metrics are why the port exists: dropped entries, batch retries and send latency say whether shipping is keeping up, and a log shipper that silently stops is the failure this makes visible. | `ansible/roles/containers/alloy/templates/docker-compose.yml.j2:171` | 2026-09-05 |
| the cap is 128M because GOGC=100 above asks for it — the 78 MB heap goal plus 8 MB of runtime plus the binary's text pages clears 96M — and for no other reason. The 2026-09-18 marker here read "raising it buys nothing the process asks for and costs a 456 MB host that already holds 320 MB in swap"; that held for the GOGC=50 pairing and the cost side still does, which is why the extra is taken as compressed swap (see the GOGC marker) rather than resident pages. The cap governs the cgroup; it reserves nothing. | `ansible/roles/containers/alloy/templates/docker-compose.yml.j2:218` | 2026-09-18 |
| ` | `ansible/roles/containers/autoheal/CLAUDE.md:47` | 2026-09-17 |
| autoheal runs with NO healthcheck, so it can never restart ITSELF (#1789). `AUTOHEAL_CONTAINER_LABEL=all` has no self-exclusion — the entrypoint filters on `health=unhealthy` alone — so the image's baked probe (`pgrep -f autoheal`, a sh+pgrep exec through runc) made autoheal its own patient. Under the load a Pi deploy puts on this 512 MB board (load1 8-17, PSI full 20-25%, swap 350 MiB+ in every episode), that exec stalls past Docker's 30s timeout three times running, autoheal reads itself `unhealthy` and issues `docker restart` on its own container, and the `start` half then fails at the OCI runtime ("Timeout waiting for systemd to create scope"). A failed start does not fire `restart: unless-stopped` — that policy covers a process that EXITS — so autoheal sat at Exited/RestartCount 0 until pi-recovery-health's `docker start` arm ran. Observed directly on 2026-09-04 18:47Z ("Container /autoheal found to be unhealthy - Restarting container now", at the window's load1 peak of 17.58); the 2026-08-29 and 2026-09-02/03 stops carry the same load signature and the same failed-start shape. Nothing is lost by dropping the probe: `pgrep` only ever detected a dead process, which `restart: unless-stopped` and the cron's `docker start` both already cover, and a wedged-but-running shell loop passed it anyway. ENFORCED by ansible/tests/services/test_autoheal_does_not_watch_itself.py. | `ansible/roles/containers/autoheal/templates/docker-compose.yml.j2:43` | 2026-09-17 |

## scripts

| Marker | File | Decided |
|---|---|---|
| the stamp is read, and both halves are shipped. `--list-only` weekly under the `etcd-snapshot` tag (k3s_etcd_restore_drill_cron, Monday 10:20), run in place from the primary checkout, since 2026-08-28 (PR #531). The fail-closed staleness reader is `check_etcd_restore_drill` in monitor-bridge/check.py, since PR #535: it reads `last-success-list-only` from this STAMP_DIR and pushes the `etcd Restore Drill` Kuma monitor. | `scripts/backup/etcd_restore_drill.sh:163` | 2026-09-02 |
| ownership is this lock, never the pid in the directory name. playbook in a backgrounded subshell whose `$$` is still the PARENT's pid, and the parent exits as soon as it has backgrounded the job -- so for the whole life of a detached deploy the name's pid is dead, and a pid-based reaper (even one run by `--check`) deleted the worktree out from under the running playbook. A process cannot lie about holding a flock. (ADR-0017) | `scripts/deploy_tools/deploy_locked.sh:202` | 2026-09-24 |
| the lock order is whatever `deploy_locks.py plan` prints, taken top to bottom, and this wrapper neither names a lock nor sorts a tag. The deployer walks the same `plan()` for its own locks, so the two orders are one function rather than two agreeing ones -- `all` first (shared for a scoped run, exclusive for a full one), then each tag in code-point order, for the reasons the marker of the same name in deploy_locks.py gives. Across the two deploy paths: this wrapper takes the tree lock, snapshots, RELEASES the tree lock and only then takes service locks, while the GitOps deployer takes the tree lock and holds it across its service locks. There is no cycle because this wrapper never re-takes the tree lock after releasing it. (ADR-0017) | `scripts/deploy_tools/deploy_locked.sh:452` | 2026-09-24 |
| the two halves of this gate come from different trees, and that is accepted. `--cwd "$snapshot"` makes probe.py render the DEPLOYED commit's manifests, while the notifier script itself -- and so its `NOT_APPLICABLE_MARKERS`, the list that turns a probe message into `skipped` -- is the CALLING checkout's copy. The marker list is the notifier's own vocabulary for reading probe output, not a fact about the deployed tree, so answering it from the tree that is running the notifier is the right source. The failure it admits needs ONE commit to reword a probe health message and its marker together, deployed by a checkout that does not yet carry the reword; the same split was ruled met on the land path, where land_lib gates from a snapshot with its own code from the primary. | `scripts/deploy_tools/deploy_locked.sh:768` | 2026-09-24 |
| this preflight fails OPEN. clear the cache, the deploy proceeds and dies at Gathering Facts exactly as it does today, except now with this script's stderr naming the cache directly above the misleading module error. Blocking every deploy on a bug in a cache-cleaner would be a worse failure than the one it prevents. | `scripts/deploy_tools/deploy_run.py:166` | 2026-09-24 |
| land.sh never deploys to daniel-stage. libvirt NAT network, which daniel-box cannot route to, and the deployer's staging gate owns every deploy there (`staging_gate.py`). `tags_by_host` still lists it, because it answers "who declares this tag" and staging does; only the landing shape drops it. Without this, landing any of the six STAGING_SUBSET tags (traefik, authelia, node-exporter, ...) ran `deploy.sh -e target=daniel-stage` from daniel-box after the box's own deploy had succeeded, and the unreachable host turned a good landing into `deploy-failed`. | `scripts/deploy_tools/deploy_tags.py:513` | 2026-09-03 |
| a usage error is not a landing, so it annotates nothing. SystemExit(2) before a Landing or a Ledger exists, so the only line this could write is `pr=unknown verdict=aborted` -- a row meaning "you typed the command wrong" in the same stream the Landings board counts. The bash original annotated it because its EXIT trap was installed before the arg loop, and this port reproduced that until issue #1304 measured 592 such rows in Loki's 744h window. `--help` (exit 0) never annotated. Nothing wraps parse_args now: its SystemExit propagates untouched. | `scripts/deploy_tools/land.py:102` | 2026-09-06 |
| exit 4 under `--at` means a commit MERGED AFTER this one reaches the same tags, and that newer landing owns the service. Deploying the merge commit anyway would revert their change; deploying the primary's HEAD would deploy a tree this landing never CI-gated. So this one falls back to the path it had before `--at` existed -- wait for the tick to fast-forward the primary onto the newest green commit, then deploy from there -- and the retry loop below is that path, unchanged. | `scripts/deploy_tools/land_lib/deploy.py:318` | 2026-09-16 |
| the tick is kicked HERE, after deploy.sh has returned, not in step 4. `gitops-deploy.service` wraps its whole unit run in the tree lock, so a tick kicked before the deploy is one deploy.sh then queues behind inside its own `flock -w 3300` -- the same wait, moved out of `tick=` and into `lock=`. Kicked after, it converges the primary while this landing gates, and the gate's own snapshot needs no lock. Not on the fallback arm: the retry loop runs a tick WITH its wait, and two requests for the same work is one more than the path had before `--at`. | `scripts/deploy_tools/land_lib/deploy.py:335` | 2026-09-16 |
| a failing retick here ENDS the landing (deploy-failed, cause=tick-failed) rather than carrying on to deploy_by_host the way bash's stale-retry loop did -- bash discarded the tick's own exit code and kept going regardless. Deliberate per tick.py's module docstring and #1013: this shares tick.py's one retry implementation with step 4 rather than land.sh's un-retried, un-accounted copy, and that implementation's failure mode is to raise. Listed as #1085 item 8 so it is not re-derived as a parity bug. | `scripts/deploy_tools/land_lib/deploy.py:391` | 2026-09-04 |
| this reads the tag slot, which | `scripts/deploy_tools/land_lib/landing.py:272` | 2026-09-19 |
| this samples on every attempt, including an uncontended first one, where bash's `note_lock_contention` only ran fuser+ps after an attempt had already lost the lock. That is a real parity delta (#1085 item 4) and it stays: reverting to bash's post-failure sample reintroduces the #1031 race this pre-sample exists to close, and `test_the_lock_holder_is_sampled_before_the_attempt` (tests/test_land_tick.py) plus its deploy.py sibling would go red on the revert. The cost is two short-lived processes per attempt against a 10-15 minute landing. | `scripts/deploy_tools/land_lib/landing.py:318` | 2026-09-04 |
| the fast path is skipped when THE TICK IS THE APPLY for part of this range -- `tick_is_the_apply`, which is `self_applied or remaining_setup`. It is not skipped for every broad prefix: a `Landing.plane` that is not self-applied is by definition what a HAND applies, so no tick can settle it and a tags-plus-plane PR keeps the fast path. A landing the predicate catches awaits the tick here exactly as it did before the fast path existed, and step 5 deploys from the primary checkout with no `--at`. | `scripts/deploy_tools/land_lib/pipeline.py:73` | 2026-09-17 |
| called AFTER `deploy.sh` returns, not before it. | `scripts/deploy_tools/land_lib/tick.py:33` | 2026-09-16 |
| a non-zero exit here is LOGGED AND IGNORED, where `run_tick` below ends the | `scripts/deploy_tools/land_lib/tick.py:39` | 2026-09-16 |
| a second `systemctl start` after the gate, rather than a deferred one. | `scripts/deploy_tools/land_lib/tick.py:77` | 2026-09-17 |
| NO tree lock is taken. | `scripts/deploy_tools/land_lib/tools.py:261` | 2026-09-16 |
| under `tempfile.mkdtemp`, NOT under HOMELAB_DEPLOY_SNAPSHOT_ROOT. | `scripts/deploy_tools/land_lib/tools.py:269` | 2026-09-24 |
| the parent directory is PINNED to GATE_TMP_ROOT rather than left to `mkdtemp`'s | `scripts/deploy_tools/land_lib/tools.py:279` | 2026-09-16 |
| the tag is dropped, not only the shared-role note. current manifests on its next image bump anyway, and a pytest guard reaches no cluster. | `scripts/deploy_tools/land_tags.py:163` | 2026-09-11 |
| `except Exception`, because any failure here must print the role tag. derivation shells out to git and decodes the output. An escape would kill a landing that has already merged, over a note the whole-role tag answers correctly. | `scripts/deploy_tools/land_tags.py:275` | 2026-09-24 |
| report the shared plane, do not fan it out to its dependents. include k8s/manifests, so a fan-out is a full deploy wearing a tag list — 20 minutes of run time, and every rollout gate and stabilisation window with it — for what is usually a two-line change. Reporting it keeps the operator's choice of when. deploy_logic.k8s_remediation reached the same conclusion for the deployer's alert path and carries the longer argument, including why routing it to `cs.broad` is worse than either. Passing ONLY the shared half. k8s_remediation appends a scoped `--tags` line for any deployable role it is given, and land.sh has already deployed those itself. | `scripts/deploy_tools/land_tags.py:334` | 2026-08-29 |
| fire on ANY change to secrets.yml, and never try to name which keys moved. Naming them means decrypting both revisions, and no plaintext may reach a terminal, a transcript or a log. `.gitattributes` sets `diff=sops`, so even `git diff ansible/vars/secrets.yml` renders the values, which is why a hook denies that form. Over-firing on a key that needed no redeploy costs one exit code; under-firing leaves a rotated credential stale in a consumer this file list cannot show. | `scripts/deploy_tools/land_tags.py:384` | 2026-09-01 |
| fire on the tag-carrying path too, which is the opposite of what deploy_logic.alert_secrets_deferred does for the deployer. Not a contradiction, a different reader: that alert is unattended, so a false fire on the /add-secret happy path is noise nobody can act on. Here an operator is reading land.sh's output, and a PR shipping secrets.yml WITH one consuming template still cannot show the OTHER consumers -- PR #695's token had two, in two planes, and the landing got neither. | `scripts/deploy_tools/land_tags.py:391` | 2026-09-01 |
| `sidecar=` is the seam that makes the CLI testable, not scaffolding a better test would avoid (#2363). Pre-#2324 `main(argv)` read no `manual_plane_tags` marker and consulted no narrowing at all (`land_tags.py` at `d2ef719b~1`), so no input separates the CLI defect from the signature — the check and its seam landed together. What the test has to hold is today's behaviour, and it does: replacing the `confirmed_narrow_tags` call in `land_tags.main` with a bare `parse_manual_plane_tags` fails `test_the_plane_cli_does_not_print_a_stale_row` on its own assertion, measured 2026-09-24. | `scripts/deploy_tools/tests/test_land_quotes_the_deployers_narrowing.py:117` | 2026-09-24 |
| these two drive `narrow_setup.main` with the argv `deploy_narrow.narrow_setup_argv` builds, and that function is the capability — there is no earlier signature to separate the defect from (#2363). The defect they name is a flag mismatch across the subprocess boundary, and they catch it: spelling `--role-tag` as `--role-tags` in the builder fails both with argparse's `unrecognized arguments`, measured 2026-09-24. | `scripts/deploy_tools/tests/test_narrow_setup.py:314` | 2026-09-24 |
| `role_tags`' `playbook` argument IS the capability under test, so no input separates this rule from the signature (#2363). `foreign_tags` needs the playbook to know which other roles are in scope; a `role_tags` without it cannot refuse a shared tag at all. Today's behaviour is what the pair below holds, and it does: making `role_tags` intersect against an empty set instead of `foreign_tags(...)` fails `test_a_tag_another_role_in_the_playbook_declares_is_flagged` with DID NOT RAISE and leaves its accepting half green, measured 2026-09-24. | `scripts/deploy_tools/tests/test_narrow_setup_edges.py:333` | 2026-09-24 |
| no fallback to a stale local copy when the deploy is missing. The same reasoning as `.claude/hooks/_claude_guard.py`: a private fallback keeps serving a verdict that looks current but was pinned at whatever the copy last held, and nobody sees it fall behind. Here the verdicts decide what to DELETE, and every importer's dangerous action — `prune_worktrees.py --prune`, `findings.py reap`, `fanout_place.py clean` — is a removal, so an ImportError is the KEEP direction: with the package absent nothing is reported and nothing is removed. The SessionStart banner is the one importer that must not crash, and `session-health.py` already catches the ImportError and prints it. CI, which has no dotfiles deploy, gets the readers from the `claude_worktree_stand_in` pytest plugin (`ansible/tests/`), a byte-identical copy a deployed-host test diffs against the real one. | `scripts/dev/_claude_worktree.py:10` | 2026-09-21 |
| ` note above the cleanup check) | `scripts/dev/fanout_lib/launch.py:218` | 2026-09-10 |
| no exit or timeout from this call can happen after the unit is live. `systemd-run` (without --wait/--pty/--scope) starts the transient unit and returns immediately, so this call is still running only while an earlier step (fetch, worktree add/lock, or the brief write) is — never after `systemd-run` has handed off. That's what makes an unconditional cleanup safe on a timeout, and what makes `--scope` forbidden here: it would tie the agent to this ssh connection, and a cleanup after that would remove a worktree a live unit still needs. `test_the_launch_command_folds_every_step_into_one_call_ending_in_systemd_run` asserts `"--scope" not in cmd` as the guard. | `scripts/dev/fanout_lib/launch.py:224` | 2026-09-10 |
| 2.5 GiB per agent. (claude_code defaults/main.yml derivation) with three to four `claude` processes live, so ~2 GiB each at peak; 2.5 GiB adds a pytest fan-out's worth (4 workers x ~150 MB). Confirm against claude_cgroup_memory_current_bytes / claude_cgroup_pids_current before raising. | `scripts/dev/fanout_lib/placement.py:10` | 2026-09-10 |
| the leaves are imported as `dev.findings_lib.<leaf>`, never as bare siblings. `scripts/docs/reference/backlog.py` reaches this code as `dev.findings` with only `scripts/` on sys.path, so a bare `from issue_model import ...` would raise ModuleNotFoundError under the docs-refresh cron while pytest stayed green. | `scripts/dev/findings.py:119` | 2026-09-06 |
| \`Claim:\` wins over \`Released:\`` in findings_lib/issue_model.py for the reasoning; `test_a_body_carrying_both_trailers_ages_the_claim_from_the_comment_ that_opened_it` is what fails if the two diverge. | `scripts/dev/findings_lib/claim.py:212` | 2026-09-06 |
| imported as `dev.findings_lib.<leaf>`, never as bare siblings — see the marker in findings.py. | `scripts/dev/findings_lib/claim_cli.py:29` | 2026-09-06 |
| the underscore-prefixed names below, and `_prefixed` further down, cross a module boundary on purpose. They moved out of findings.py byte-for-byte, changing no behaviour; renaming them would have turned that move into a rewrite. The underscore still carries what it did before — internal to the findings modules, not an API another script may import. Conventions for a new module: docs/python-code-organization.md. | `scripts/dev/findings_lib/issue_model.py:84` | 2026-09-05 |
| `Claim:` wins over `Released:` within ONE comment body. `claim_comment` nor `release_comment` ever emits both trailers, so a body carrying both is malformed input, and every fail-safe in this module points the same way: `is_operator_comment` folds an unattributable comment into nothing, `claim_is_live` HOLDS a claim whose worktree it cannot read, `cmd_reap` refuses on a git error, and `cmd_next` withholds. Releasing on ambiguity is the one direction that hands live work to a second session, which is the harm this protocol exists to prevent. This `continue` is what implements the choice, so it is not a tidy-up — dropping it inverts the verdict. `_claim_age_days` in findings_lib/claim.py carries the identical fold and must keep the identical skip, or a `claims` row ages a claim the register does not think exists. | `scripts/dev/findings_lib/issue_model.py:445` | 2026-09-06 |
| `git ls-files`, not `rglob` — the reason | `scripts/dev/pytest_shard.py:143` | 2026-09-06 |
| /bin/sh, not bash — cron uses sh, and reproducing only the PATH half while keeping a bash-or-zsh interpreter leaves the word-splitting trap unreproduced. | `scripts/dev/run_as_cron.sh:69` | 2026-08-27 |
| the command runs through `sh -c`, not `exec "$@"`. line — cron hands the whole thing to /bin/sh — so exec'ing argv directly would refuse every builtin, pipeline and redirect a real cron job is allowed to use, and would fail them with a 127 that reads exactly like the PATH trap this script exists to expose. One argument is taken as that shell line verbatim; several are quoted into one. | `scripts/dev/run_as_cron.sh:74` | 2026-08-27 |
| a plain redirect into a file, then cat — NOT `tee` via process substitution. The subshell tee runs in is reaped asynchronously, so the -s emptiness test below can read the file before tee has flushed and call a talkative command silent. stderr stays on stderr so a warning is not mistaken for output. | `scripts/dev/run_as_cron.sh:85` | 2026-08-27 |
| state the floor instead of leaving it to the platform default. py/insecure-protocol reads a bare default context as permitting TLSv1 and TLSv1_1, and a hand-dismissal of that alert does not survive the next file move. (ADR-0016) | `scripts/diagnostics/probe_lib/ha.py:283` | 2026-09-02 |
| deferred — `ha state` needs build_model(); ha_state_model.py:434 needs this module's ha_get/ha_token for `refresh`. Pinned in ALLOWED_CYCLES. | `scripts/diagnostics/probe_lib/ha.py:582` | 2026-09-05 |
| the ClusterIP pair below stays here, not in health_kubectl.py. and `resolve_service_ip` are kubectl by transport but Docker by role — they are the successor to `inspect_ip_argv`/`resolve_ip`, answering the same question ("reach this service directly, bypassing Traefik and Authelia") for the workloads that left the Pi. Moving them would split that pair across two modules and falsify health_kubectl.py's invariant: it imports no sibling and runs no command, so every argument shape the gate depends on is assertable without a cluster, while `resolve_service_ip` runs a command through `lib.kubectl` and imports `core`. The module NAME is what misleads a reader here, not the grouping. Enforced by test_probe_health.py:: test_health_kubectl_imports_nothing_so_its_argv_shapes_need_no_cluster. | `scripts/diagnostics/probe_lib/health_docker.py:60` | 2026-09-18 |
| `starting` with FailingStreak 0 passes the gate rather than polling until it settles. A deploy recreates the container, so the gate reads it before its healthcheck has completed a single probe and Docker reports `starting` — which failed the gate on a correct deploy (#1601). Polling instead is not available here: `deploy_detach_notify.PROBE_TIMEOUT_S` caps one `probe.py health` run at 30s, and daniel-pi sets `container_healthcheck_interval: 60s`, so no bounded poll inside that budget can reach the first probe result. What bounds the pass is Docker's own state machine: `starting` ends at the first probe result, so the window is about one interval, and a probe that has actually failed leaves a non-zero FailingStreak, which still fails here. An `unhealthy` container and a `starting` one with a failing streak are unaffected. | `scripts/diagnostics/probe_lib/health_docker.py:87` | 2026-09-10 |
| ` marker | `scripts/diagnostics/probe_lib/health_kubectl.py:12` | 2026-09-05 |
| the decrypt stays ON by default, and `no_secrets` is the opt-out — not the | `scripts/diagnostics/probe_lib/monitors.py:308` | 2026-09-10 |
| ` marker | `scripts/diagnostics/tests/test_probe_health.py:324` | 2026-09-05 |
| homepage's expectation stays `My Awesome Homepage`; do NOT relax it to `Homepage`. `Homepage` is the app's OWN fallback — `initialSettings.title \|\| "Homepage"` at `src/pages/index.jsx:410` in gethomepage/homepage v1.13.2, the revision this cluster runs — so seeing it means the page rendered with NO settings at all, which is the failure this entry exists to catch. Verified 2026-09-06 against the live pod: `__NEXT_DATA__.props.pageProps.initialSettings.title` reads `My Awesome Homepage`, and `title:` at `roles/k8s/homepage/templates/config/settings.yaml.j2:2` is what puts it there. Issue #1399 read a `Homepage` failure as a stale expectation and proposed both relaxing this line and deleting that setting; both would have made the check permanently green on the broken state. | `scripts/diagnostics/tests/test_ui_smoke.py:49` | 2026-09-06 |
| `speedtest-tracker` is not enrolled, and its `table` panel is not why `exportarr-arr-stack` is. Two of the four boards above already carry a `table` (claude-code-otel, traefik-custom), so `table` is not a new shape — flatness is. speedtest-tracker is that same flat shape carrying only `stat` and `timeseries`, both enrolled twice over, so under this list's own one-per-shape rule it adds nothing while costing another board in the single browser the 2Gi limit bounds. Re-enrol it only against a shape argument, not because it is unenrolled. Issue #1387. Re-measured live 2026-09-06 after `Download queue depth` (panel id 14) landed: the board drew 13 headers when it was enrolled, and draws 14 now. Re-measured live 2026-09-10 after `sonarr episode quality mix` (panel id 15, a flat `piechart`) landed: enrolled at 999 and read the observed count out of the failure — `the dashboard mounted but drew 15 panel header(s)`. That is how to re-measure this list; a derivation from the board's panel count only holds while it carries no `row`. | `scripts/diagnostics/tests/test_ui_smoke_grafana.py:69` | 2026-09-06 |
| 2026-09-05 nothing imports these three by name; `test_gen_infra_map.py` reaches them through re-exports, so they carry `importer` rather than `import`. | `scripts/docs/tests/test_gen_reference_scripts.py:412` | 2026-09-05 |
| module-level and one-way in practice, not deferred — checks are a pure function of the model, so this edge is the real dependency, not an artifact of split order. The reverse edge (ha_state_model.py:520, `main()`'s `check` subcommand calling check_errors) is the deferred one; see the DECIDED marker there for why it isn't worth a third module. | `scripts/home_assistant/ha_state_checks.py:34` | 2026-09-05 |
| deferred, the reverse edge of probe_lib/ha.py:581's `run_ha_state`. function needs build_model() to render `ha state`; `refresh` here needs core/ha's live-HTTP helpers (ha_base/ha_resolve/ha_token/ha_get) to snapshot external entities. Both edges stay inside the one function that needs them, so neither runs at import time. Fixing it means carving the HA HTTP client out of probe_lib.ha into a module neither side owns — real restructuring, so it is pinned in ALLOWED_CYCLES instead. | `scripts/home_assistant/ha_state_model.py:433` | 2026-09-05 |
| deferred, the reverse edge of ha_state_checks.py:34's module-level import of this module's build_model/DERIVED_YAML/etc. That import is real and load-bearing (the checks are a pure function of the model), so it cannot move here instead. This one is deferred only because it exists to serve the CLI's `check` subcommand — every other subcommand (generate/refresh) never touches ha_state_checks at all. Moving the CLI dispatch itself into a third module would break the cycle but turns a working two-module split into three for one subcommand; pinned in ALLOWED_CYCLES instead. | `scripts/home_assistant/ha_state_model.py:526` | 2026-09-05 |
| ` marker gives the reason — importing `ansible.plugins.filter.core` costs ~190 ms and | `scripts/lib/ansible_jinja_compat.py:9` | 2026-09-24 |
| `` marker gives: ``probe_lib/monitors.py`` reaches that module and | `scripts/lib/ansible_jinja_env.py:21` | 2026-09-24 |
| not age-gated, unlike `behind_park_lines` and monitor-bridge's `gitops_status`. | `scripts/lib/deployer_park.py:181` | 2026-09-24 |
| {c.selector}" in ln | `scripts/lib/facts/atoms.py:183` | 2026-09-19 |
| (?P<sel>\S.*)$")), | `scripts/lib/facts/citations.py:50` | 2026-09-19 |
| this skips the whole unit, not only the hash comparison. would still be valid here — set membership does not depend on the interpreter — but its remedy is the same `verify` run, and one actionable finding beats several. | `scripts/lib/facts/lock.py:220` | 2026-09-19 |
| the shim is `ansible_jinja_compat.ansible_bool`, not ansible-core's `to_bool` that `lib.ansible_jinja_env.register_ansible_filters` binds for every render guard. `to_bool` would make the two paths agree by identity, but importing `ansible.plugins.filter.core` costs ~190 ms and `probe_lib/monitors.py` imports this module — `probe.py monitors` reaches no ansible-core module today (measured 2026-09-18, `python -X importtime`). The shim copies `to_bool`'s tables and fallback, and `test_ansible_bool_agrees_with_ansible_core_to_bool` pins the copy to the real filter, so the two paths agree by test rather than by identity (#2074). | `scripts/lib/k8s_context.py:54` | 2026-09-18 |
| kept although the tree holds no hyphenated script since #2386 deleted `glenstone-bot.py` and `osteria-francescana-bot.py`, the two this branch was written for. Dropping it leaves a future hyphenated script falling through to the path-mention rule, which `_is_another_scripts_test` then discards — the page would call it untested, the exact miscredit this branch closed. Three lines against a silent wrong answer; `test_a_hyphenated_script_is_credited_to_the_test_that_loads_it` keeps it honest. | `scripts/lib/script_coverage.py:103` | 2026-09-24 |
| ` marker costs ~190 ms if this module leaks into it. | `scripts/lib/tests/test_ansible_jinja_env.py:101` | 2026-09-24 |
| marker in " | `scripts/lib/tests/test_ansible_jinja_env.py:122` | 2026-09-24 |
| a fixed slice while volume names vary\ny = 2\n", | `scripts/lib/tests/test_facts_atoms.py:203` | 2026-09-19 |
| a fixed slice", "m.py", "a fixed slice") | `scripts/lib/tests/test_facts_atoms.py:205` | 2026-09-19 |
| a fixed slice while volume names vary\n", | `scripts/lib/tests/test_facts_atoms.py:210` | 2026-09-19 |
| a fixed slice while volume names differ\n") | `scripts/lib/tests/test_facts_atoms.py:213` | 2026-09-19 |
| a\n# DECIDED: a b\n") | `scripts/lib/tests/test_facts_atoms.py:218` | 2026-09-19 |
| a", "m.py", "a"), tmp_path) | `scripts/lib/tests/test_facts_atoms.py:220` | 2026-09-19 |
| zzz", "m.py", "zzz"), tmp_path) | `scripts/lib/tests/test_facts_atoms.py:222` | 2026-09-19 |
| a fixed slice while`" | `scripts/lib/tests/test_facts_citations.py:79` | 2026-09-19 |
| keep it\n") | `scripts/lib/tests/test_facts_lint.py:23` | 2026-09-19 |
| keep it`.\n" | `scripts/lib/tests/test_facts_lint.py:38` | 2026-09-19 |
| keep`\n") | `scripts/lib/tests/test_facts_lint.py:85` | 2026-09-19 |
| keep it\n# DECIDED: keep that\n") | `scripts/lib/tests/test_facts_lint.py:86` | 2026-09-19 |
| keep the cap\n") | `scripts/lib/tests/test_facts_lock.py:188` | 2026-09-19 |
| keep`") | `scripts/lib/tests/test_facts_lock.py:190` | 2026-09-19 |
| keep the cap\n# DECIDED: keep the floor\n" | `scripts/lib/tests/test_facts_lock.py:194` | 2026-09-19 |
| ` marker at the branch for why it | `scripts/lib/tests/test_script_coverage.py:316` | 2026-09-24 |
| a commit reads as a whole-file re-encrypt when it changes the ciphertext of at least 90% of the keys the two revisions share, and at least 10 keys are shared (so a file with only a handful of secrets can't hit the fraction on an ordinary rotation). Measured: 3e731bcec (a merge-conflict re-encrypt) changed 165 of 166 shared keys (99.4%); every other commit in the last 25 changed 0-2 (<=1.2%). 90% sits far above that noise floor and well below "half the registry rotated at once", which a real bulk rotation could plausibly do and this guard must not swallow. | `scripts/secrets_mgmt/git_dates.py:130` | 2026-09-09 |
| git evidence beats the seed even though it can overstate freshness for a credential minted before this file's first commit (2026-01-17) — such a secret dates to when it was committed, not when it was created. The seed it replaces is not a better reading: `seed_last_rotated` backdates by a hash of the NAME, so it is fiction for every secret nobody has rotated since registration. That fiction is what aged calendar_1 into a false OVERDUE and took the monitor down on 2026-08-25. | `scripts/secrets_mgmt/git_dates.py:264` | 2026-09-05 |
| `email` was the first candidate for this set (#1183) and went to the registry's `ignore` tier instead, because it is an address rather than a credential — it renders as an ACME contact, a git commit author, an Authelia user's email and an SMTP username, and the value that authenticates that SMTP session is `smtp_notify_app_password`, tracked separately. That is the `domain` precedent NON_SECRET_TIERS below already states: when the registry can say a name is not a credential, it says it, and this derivation reads it. GENERIC_NAMES stays for the case the registry CANNOT express — a name that really is a credential but whose word is too common to match on. A registry comment could not have carried this: `rotation_tools.save_registry` rewrites the file with `yaml.safe_dump`, so the weekly rotate cron drops any comment added there. | `scripts/secrets_mgmt/secret_bearing_host_paths.py:54` | 2026-09-05 |
| 0700` marker for the same reason, and until | `scripts/secrets_mgmt/secret_bearing_host_paths.py:181` | 2026-09-17 |
| 0700` marker for it and twelve did not, and three of those | `scripts/secrets_mgmt/tests/test_secret_bearing_host_scripts_are_not_world_readable.py:5` | 2026-09-17 |
| ` at probe_lib/ha.py:581 and at ha_state_model.py's `cmd_refresh` (the deferred `from diagnostics.probe_lib import core`/`ha`). | `scripts/tests/test_scripts_import_direction.py:102` | 2026-09-05 |
| ` at ha_state_checks.py's `from ha_state_model import (...)` and at ha_state_model.py's `main()` (the deferred `from ha_state_checks import check_errors`). | `scripts/tests/test_scripts_import_direction.py:107` | 2026-09-05 |
| role defaults are merged LAST here, so they outrank the inventory — the reverse of Ansible's own precedence, where role defaults are the weakest layer. The inversion is held harmless by `colliding_default_keys` above rather than corrected, because swapping the merge order changes the render context of every role at once to fix a collision that does not exist. Full reasoning in that function's docstring. Contradict it with a case where the guard passes and the render is still wrong. | `scripts/validate/k8s_manifests.py:267` | 2026-08-28 |
| redirect_stderr/redirect_stdout, not capsys. pytest refuses to inject it into a module-scoped fixture; main() writes with print(file=sys.stderr), which redirect_stderr captures because it rebinds sys.stderr at call time. | `scripts/validate/tests/test_validate_k8s_manifests.py:36` | 2026-09-01 |
| ` in | `scripts/validate/tests/test_validate_shell_templates.py:87` | 2026-09-19 |

## docs

| Marker | File | Decided |
|---|---|---|
| ` markers sat at the lines they governed, `docs/archive/` held superseded | `docs/adr/0001-mkdocs-site-with-generated-reference.md:36` | 2026-08-24 |
| contention exits 75` marker in | `docs/adr/0011-one-lock-serialises-every-deploy-path.md:68` | 2026-09-12 |
| ` marker recording the lock order in | `docs/adr/0017-the-tree-lock-guards-the-tree-not-the-cluster.md:133` | 2026-09-18 |
| ` comment at the code line it governs — that is | `docs/adr/index.md:9` | 2026-08-24 |
| ` literal and carries the reference after it — | `docs/adr/index.md:15` | 2026-08-24 |
| … (ADR-0011)`, never `# DECIDED (ADR-0011):`. | `docs/adr/index.md:16` | 2026-08-24 |
| ` marker referencing this record. | `docs/adr/template.md:45` | 2026-08-24 |
| ` literal and puts the reference after it**, like this: | `docs/adr/template.md:48` | 2026-08-24 |
| 8 chars, not 12 — minimum-not-width, and the assert fires before the scale-down. (ADR-0011) | `docs/adr/template.md:51` | 2026-08-24 |
| '` before flagging anything in a role, and that grep is literal — moving | `docs/adr/template.md:56` | 2026-08-24 |
| the `slice-` prefix in `k3s-migration/` stays, against the rule above.** Those | `docs/archive/README.md:12` | 2026-09-02 |
| ` marker at the line it governs. | `docs/archive/README.md:26` | 2026-08-24 |
| ` markers already in the code, a scoped Vale style gate, and D2 for hand-authored diagrams. | `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:5` | 2026-08-24 |
| ` marker whose reasoning outgrew its line gains an `ADR-NNNN` reference, and `ansible/tests/repo/test_adr_links.py` enforces both directions. | `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:7` | 2026-09-02 |
| ` marker convention is defined in the repo-root `CLAUDE.md`** under *Review & Memory Hygiene*, and `.claude/skills/homelab-review/SKILL.md` step 3 greps for it. | `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:19` | 2026-08-24 |
| … ADR-NNNN` marker pointing back here. | `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:75` | 2026-08-24 |
| ` comment at the code line it governs — | `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:100` | 2026-08-24 |
| ' marker where a reviewer trips over it. | `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:141` | 2026-08-24 |
| " --include="*.py" --include="*.yml" --include="*.j2" --include="*.sh" . | `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:162` | 2026-08-24 |
| ' markers must reference each other, in both directions. | `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:170` | 2026-08-24 |
| ' markers at the lines they govern, which .claude/skills/homelab-review/ | `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:173` | 2026-08-24 |
| ' link in both directions | `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:309` | 2026-08-24 |
| ` markers. | `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:329` | 2026-08-24 |
| " --include="*.py" --include="*.yml" --include="*.j2" --include="*.sh" . | `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:344` | 2026-08-24 |
| 8 chars, not 12 — minimum-not-width, and the assert fires before the scale-down. | `docs/archive/docs-ui-and-adrs/2-adrs-and-style.md:372` | 2026-08-24 |
| ` markers | `docs/archive/docs-ui-and-adrs/design.md:7` | 2026-08-24 |
| ` | `docs/archive/docs-ui-and-adrs/design.md:27` | 2026-08-24 |
| ` markers | `docs/archive/docs-ui-and-adrs/design.md:137` | 2026-08-24 |
| ` markers sit across 20 files at the lines | `docs/archive/docs-ui-and-adrs/design.md:139` | 2026-08-24 |
| ` marker sits | `docs/claude-shell-permissions.md:98` | 2026-09-18 |
| the lock order is` note exists only because bash cannot import | `docs/deploy-sh-python-port.md:23` | 2026-09-24 |
| this preflight fails OPEN` note requires. | `docs/deploy-sh-python-port.md:143` | 2026-09-24 |
| the two halves of this gate come from different trees` split exactly: notifier code from the caller, probe renders from the snapshot. | `docs/deploy-sh-python-port.md:144` | 2026-09-24 |
| ` markers and regenerates on | `docs/deploy-sh-python-port.md:254` | 2026-09-24 |
| ` marker above `_BROAD_MANUAL_PREFIXES` in `deploy_logic.py` carries the evidence. | `docs/gitops-pipeline.md:113` | 2026-09-01 |
| ` marker. | `docs/gitops-pipeline.md:449` | 2026-09-21 |
| ` says it has to run. | `docs/gitops-pipeline.md:579` | 2026-09-24 |
| ` measured; and the range merges below, so `skip_hold` could | `docs/gitops-pipeline.md:587` | 2026-09-24 |
| ` at the fallback in | `docs/gitops-pipeline.md:639` | 2026-09-21 |
| ` on | `docs/gitops-pipeline.md:645` | 2026-09-21 |
| ` marker and the measurement. | `docs/gitops-pipeline.md:700` | 2026-09-21 |
| ` marker above `_BROAD_MANUAL_PREFIXES` in | `docs/gitops-pipeline.md:751` | 2026-09-21 |
| ` at the `cs.k8s` branch of | `docs/gitops-pipeline.md:958` | 2026-09-21 |
| ` at that task says why the state | `docs/gitops-pipeline.md:1104` | 2026-09-21 |
| ` marker in | `docs/gitops-pipeline.md:1541` | 2026-09-21 |
| ` marker in | `docs/monitor-bridge-checks.md:445` | 2026-09-21 |
| 12 cycles` marker | `docs/monitor-bridge-checks.md:472` | 2026-09-21 |
| ` marker in `bridge/config_io.py`) over the window; the message names which reason fired when the | `docs/monitor-bridge-checks.md:1007` | 2026-09-21 |
| ` marker | `docs/post-merge-automation.md:268` | 2026-09-17 |
| ` marker in the tree. | `docs/reference/scripts.md:42` | 2026-09-03 |
| ` block records that with the remote-control unit | `docs/self-hosted-runner-spike.md:179` | 2026-09-22 |
| ` block has to be redone: | `docs/self-hosted-runner-spike.md:190` | 2026-09-22 |
| ` marker above the retirement tasks in `roles/setup/gitops_deploy/tasks/install.yml`. | `docs/staging-phase-c.md:526` | 2026-09-24 |

## other

| Marker | File | Decided |
|---|---|---|
| no fallback to a stale local copy when the deploy is missing. slice-3 ledger's #484 half-deploy (~/.claude/artifacts/claude-guard-slice3/sdd-ledger/ progress.md) is why: a machine mid-deploy can have the new hook code without yet having the dotfiles package behind it, and a private fallback copy would keep serving a permission decision that looks current but was pinned at whatever the fallback last held — nobody sees that it fell behind. This module raises; `_hook_common.py` catches it and reports the segmenter as missing, and each guard turns that into its own posture on an unreadable command. | `.claude/hooks/_claude_guard.py:19` | 2026-09-17 |
| every arm runs under its own `try/except`, and an arm that raises loses its verdict | `.claude/hooks/bash-pretool.py:36` | 2026-09-24 |
| the `cd` arm asks; every other failure stays fail-open. three failure paths fail-open so that a broken guard does not brick every tool call, and gave the `cd` arm the stderr line the other two already had. An `ask` is a prompt, not a brick: the operator reads why a DENY guard could not run and decides once, where a bare `exit 0` disarmed it in silence (#2171). `uv` missing or the .py missing still exit non-zero with their own stderr line — a failed `exec` ends a non-interactive shell, so no `ask` can follow it. #2394 made one shim carry a posture that used to be split: three of the five arms asked on a failed `cd` and two stayed silent, and a single process can only do one thing, so it asks and the reason names all three guards that did not run. The two silent arms cost a missed approval and a missed doc injection, which is a prompt and a re-read, not a bypass. | `.claude/hooks/bash-pretool.sh:21` | 2026-09-24 |
| the `cd` arm asks; the other two arms stay fail-open. fail-open so that a broken guard does not brick every tool call, and gave the `cd` arm the stderr line the other two already had. An `ask` is a prompt, not a brick: the operator reads why a DENY guard could not run and decides once, where a bare `exit 0` disarmed it in silence (#2171). `uv` missing or the .py missing still exit non-zero with their own stderr line — a failed `exec` ends a non-interactive shell, so no `ask` can follow it. | `.claude/hooks/block-footguns.sh:9` | 2026-09-21 |
| arm 3 runs FIRST, and denies where arm 1 only asks. sed -i s/a/b/ ansible/vars/secrets.yml` matches both, and an `ask` there would let the escape be approved on the strength of a prompt about the wrong thing. The asymmetry arm 1's docstring argues does not apply here: a heuristic that wrongly asks costs a prompt, but a write outside an isolated session's worktree is never the right call, so a wrong deny costs one re-run from the right directory while a wrong allow parks the deployer for every session. The known cost is that a deliberate edit to the chezmoi checkout from a server worktree is denied; the reason string names the way through. | `.claude/hooks/block-protected-bash.py:371` | 2026-09-06 |
| the process cwd, not `claude_guard.hook.read_cwd`'s `""`, when the payload carries no `cwd`. Every arm joins `repo_root` as a path prefix — `os.path.join` for a relative write target and for `scripts/secrets_mgmt`, `relpath` inside `classify` — and an empty prefix resolves against the process cwd implicitly, the same silent probe `read_cwd`'s docstring refuses for `git -C ""`. Naming the directory says which checkout those paths resolve in. The shim's `cd /home/ubuntu/server` makes it the primary checkout, a real repo root, so arms 1 and 2 keep a tree to read; arm 3 reads the same thing either way, because `_in_a_worktree` of the primary checkout is False and the escape guard is inert on a missing `cwd` under both spellings. Issue #2135. | `.claude/hooks/block-protected-bash.py:407` | 2026-09-21 |
| the `cd` arm asks; the other two arms stay fail-open. fail-open so that a broken guard does not brick every tool call, and gave the `cd` arm the stderr line the other two already had. An `ask` is a prompt, not a brick: the operator reads why a DENY guard could not run and decides once, where a bare `exit 0` disarmed it in silence (#2171). `uv` missing or the .py missing still exit non-zero with their own stderr line — a failed `exec` ends a non-interactive shell, so no `ask` can follow it. | `.claude/hooks/block-protected-bash.sh:10` | 2026-09-21 |
| the `cd` arm asks; the other two arms stay fail-open. fail-open so that a broken guard does not brick every tool call, and gave the `cd` arm the stderr line the other two already had. An `ask` is a prompt, not a brick: the operator reads why a DENY guard could not run and decides once, where a bare `exit 0` disarmed it in silence (#2171). `uv` missing or the .py missing still exit non-zero with their own stderr line — a failed `exec` ends a non-interactive shell, so no `ask` can follow it. | `.claude/hooks/block-protected-edits.sh:12` | 2026-09-21 |
| fail-open, not fail-closed, on all three ways this shim can break — a broken guard must not brick every tool call. `uv` missing or the .py missing already write their own line to stderr; only the `cd` arm was silent, so it now matches them. See issue #1014. | `.claude/hooks/inject-nested-docs.sh:9` | 2026-09-21 |
| the `cd` arm asks; the other two arms stay fail-open. fail-open so that a broken guard does not brick every tool call, and gave the `cd` arm the stderr line the other two already had. An `ask` is a prompt, not a brick: the operator reads why a DENY guard could not run and decides once, where a bare `exit 0` disarmed it in silence (#2171). `uv` missing or the .py missing still exit non-zero with their own stderr line — a failed `exec` ends a non-interactive shell, so no `ask` can follow it. | `.claude/hooks/nudge-land-sh.sh:8` | 2026-09-21 |
| this | `.claude/hooks/tests/test_block_footguns_skip_staleness.py:107` | 2026-09-21 |
| ` marker recording the trade-off is present (so a future session does | `.claude/hooks/tests/test_hook_shim_fail_open.py:15` | 2026-09-21 |
| ` block, which records the trade-off once for the whole class. Three of the four became arms of `bash-pretool.sh` in #2394, so that one shim now answers for all three — and its `ask` reason names each of them, because an operator reading one line needs to know which guards did not run. | `.claude/hooks/tests/test_hook_shim_fail_open.py:40` | 2026-09-24 |
| " in text | `.claude/hooks/tests/test_hook_shim_fail_open.py:145` | 2026-09-03 |
| marker, cited by text prefix | `.claude/rules/facts.md:29` | 2026-09-21 |
| a fixed slice while | `.claude/rules/facts.md:30` | 2026-09-21 |
| ` marker makes the alternative fail. | `.claude/skills/homelab-review/SKILL.md:75` | 2026-08-29 |
| ` rule**: before flagging anything in a role, `grep -rn '# DECIDED:'` that role's | `.claude/skills/homelab-review/SKILL.md:120` | 2026-08-22 |
| ` marker records a trade-off that was analysed and settled, | `.claude/skills/homelab-review/SKILL.md:121` | 2026-08-22 |
| ` marker makes the | `.claude/skills/homelab-review/SKILL.md:340` | 2026-08-29 |
| ` marker at the line | `.claude/skills/homelab-review/accepted-designs.md:11` | 2026-09-21 |
| ansible-lint gets its own job, again. 2026-08-14, and restored it on 2026-09-10 (#1332). The revert is not a precedent to re-apply, because the two facts it rested on have both gone: | `.github/workflows/ci.yml:84` | 2026-09-10 |
| ` at the line it governs.** A decision recorded only in a | `CLAUDE.md:253` | 2026-08-22 |
| `origin\[:8\]`' | `CLAUDE.md:257` | 2026-09-18 |
| daniel-server's key ALONE — not daniel-box's, and not the off-box recovery key. `pre_tasks/load_secrets.yml` decrypts with `delegate_to: localhost`, so the CONTROLLER decrypts, and daniel-server is the only host that can route to the staging guest (docs/staging-cluster.md, Decision 2). No recovery key because there is nothing to recover: staging holds generated values and no real credential, so a lost key means regenerate. | `ansible/.sops.yaml:29` | 2026-08-27 |
| cni0 alone, for netpol_baseline_node_cidrs. `netpol_baseline_node_cidrs` reads the pair above and NOT the flannel pair below, deliberately. A host process reaching a baseline-selected pod on the other node is denied — measured 2026-09-10 (#1725) with the pod's node as the only variable: from daniel-box, sonarr's ClusterIP (pod on daniel-box) answered 200 while prowlarr's (pod on daniel-server) timed out, and from daniel-server the two verdicts swapped. The census found ONE host caller of that shape, `get_via_service()` in scripts/diagnostics/postflight.py, and it already falls back to the workload's Traefik route when the ClusterIP does not answer (#1633) — which is why the *arr `-monitoring` routes had to admit the flannel pair in #1697. Traefik itself is immune: its policy's first ingress rule carries ports 8000/8443 with no `from:`, so the three host crons that dial it are unaffected by this list. Adding the flannel pair here would re-scope every pod the namespace-scoped baseline selects, to buy a fast path that already has a working fallback. Contradict this with a NAMED host caller that has no route fallback. The two nodes' flannel.1 VXLAN addresses — the OTHER source a host process can arrive as. Which of the two sets a host request carries depends on where the traefik pod sits: traffic to a pod on the SAME node leaves via cni0 and arrives as that node's cni0 gateway above, while traffic to a pod on the OTHER node crosses the overlay and arrives as the sending node's flannel.1 address. Traefik is pinned to daniel-box, so daniel-box arrives as 10.42.0.1 and daniel-server as 10.42.1.0 — measured 2026-09-10 (#1693): from a daniel-server shell `ip route get` to a daniel-box pod reads `src 10.42.1.0`, and the same day's #1683 measurement read `ClientHost: 10.42.1.0` off Traefik's access log. | `ansible/inventory/group_vars/all.yml:110` | 2026-09-11 |
| 10G fleet cap, derived the way defaults/main.yml derives daniel-box's 12G. Measured on daniel-server 2026-09-09. Re-derive all of it if you change this: RAM: MemTotal 32,478,080 kB = 31.0 GiB. Unreclaimable footprint, as cgroup anon: kubepods.slice 4.98 GiB + machine.slice (the daniel-stage KVM guest) 4.75 GiB + system.slice 0.58 GiB = 10.3 GiB. Budgeted at 14.4 GiB (+40%) so k3s and the guest can grow without racing the fleet. Kernel: SUnreclaim 0.48 GiB. Page cache left free for Longhorn I/O: ~3 GiB. 31.0 - 14.4 - 0.5 - 3 = 13.1 GiB available to the fleet. 10G leaves ~3 GiB unspent, the same margin daniel-box keeps, because the throttle stalls whichever cgroup loses the reclaim race and the homelab plane is the one that must not. With the RC unit disabled, user-1000.slice is the only plane here, so the 8G login cap is the effective per-plane bound and the fleet cap only matters once a second plane appears. | `ansible/inventory/host_vars/daniel-server.yml:33` | 2026-09-09 |
| /api is deliberately NOT routed` marker in that same template — `api@internal` prints every router's rule verbatim, and two of prod's embed credentials in `Header()` matchers. A 200 here means the SPA-fixing change that marker forbids has been made. | `ansible/inventory/host_vars/daniel-stage.yml:61` | 2026-09-10 |
| an allowlist, not `inventory_hostname not in k3s_agent_hosts`. would admit every host nobody remembered to deny, which is the same shape as the accident this assert exists to prevent. A new server node is a deliberate edit to k3s_server_hosts; ansible/tests/setup/test_k3s_server_hosts.py keeps it that way. | `ansible/k3s-bringup.yml:37` | 2026-08-27 |
| size this cap against the CFS quota per PERIOD, never against the average rate. The average is misleading here: the sidecar idles between scrapes and does all of its work in one burst every 60s, so a 24h mean of ~0.5 millicores hides a burst that a per-period quota throttles. 100m is 10ms of CPU per 100ms period; sonarr's scrape costs 25-86ms of CPU (24h mean x 60s, and the 5m peak, measured 2026-09-18), so it spilled across 4-7 periods and was throttled in ~48% of them: 0.08-0.34s of throttled time per scrape, against a scrape_duration of 0.53s vs 0.015-0.03s for radarr and prowlarr. (Issue #2017.) 500m is 50ms per period -- most sonarr scrapes fit in one, and radarr/prowlarr (throttled 1-5% at 100m) never hit it. One shared value keeps the three from drifting; it is a limit, so an idle sidecar costs nothing more. | `ansible/templates/exportarr.yml.j2:86` | 2026-09-18 |
| `git ls-files`, not `rglob`. | `ansible/tests/_helpers.py:319` | 2026-08-27 |
| shared docs, not deep copies. a doc, and copying 323 dicts per call would spend a slice of what the cache saves. A test that needs to mutate one must copy it itself — mutating in place corrupts every later test in the same worker. | `ansible/tests/_k8s_render.py:85` | 2026-08-23 |
| role defaults go under the inventory here, which is Ansible's own precedence and the reverse of `_render_all`'s. `_render_all` copies the validator, which puts the defaults on top and holds that harmless with `colliding_default_keys` — but that guard runs against daniel-box only, and daniel-stage overrides `traefik_k8s_manage_crowdsec` on purpose, so a staging render with defaults on top would render the value the deploy never uses. For daniel-box the guard makes the two orders render the same text. | `ansible/tests/_k8s_render.py:137` | 2026-09-21 |
| an entry has to MATCH its file, not merely bound it. | `ansible/tests/_ratchet.py:77` | 2026-09-05 |
| a changed guard may RAISE an entry, not only add one. patches that were always there in files that already have a line, which is what the string-target fix hit: test_session_health.py went 33 -> 36 and no exemption covered it. The cost is that `raised_entries` serves both ratchets, so a branch editing _ratchet.py may also raise a module-length entry; the bound is that the trigger is narrow (this file, its test module, or _helpers.is_test_file) and the guard diff is in the same PR a reviewer reads. | `ansible/tests/_ratchet.py:198` | 2026-09-06 |
| the string form counts, resolved against `first_party` rather than `bound`. is the object form's equal at runtime, and counting only the object form taught authors which spelling the guard could not see. The rationale and the incident are in this module's docstring. | `ansible/tests/_ratchet.py:309` | 2026-09-06 |
| an entry has to MATCH its file, not merely bound it. and the stance this reversed, are in this module's docstring. | `ansible/tests/_ratchet.py:383` | 2026-09-05 |
| ` says why there is no fallback at runtime. | `ansible/tests/claude_worktree_stand_in.py:9` | 2026-09-21 |
| the exemption is per REGISTER, not per task (#2360). evaluates a skipped task's own `failed_when`, so a producer cannot meet its own skip result — but exempting the whole task hid its reads of every OTHER producer. Keyed on the register NAME rather than task identity: no task file in this tree registers one name twice, so the two are the same rule here. | `ansible/tests/deploy/_skip_result_rule.py:154` | 2026-09-24 |
| a consumer check mode skips is not judged on its `failed_when` or its `changed_when` (#2375). Ansible evaluates both on the result a module returned, and a skipped task returns none — so a read there cannot happen on the very run that makes the producer a skip result. Its module args, its `when:` and its `until:` stay judged; `_check_mode.POST_MODULE_KEYS` says why `until:` is not in the set. The when-based rule in `_offenders` drops nothing: its producer is skipped on a REAL run, where this consumer runs, its module returns, and its `failed_when` meets the skip dict. | `ansible/tests/deploy/_skip_result_rule.py:202` | 2026-09-24 |
| the class is accepted for the services whose regression is cheap, and fenced by the auto-deploy denylist for the ones whose regression is not. Censused 2026-09-10: of the 13 k8s roles carrying this pin shape, the three #1524 named as expensive — qbittorrent, tdarr, healthchecks — were ALREADY denied auto-deploy, as were cloudflare-ddns and dri-device-plugin. The eight that remain eligible are stateless front-ends and probes on `:latest`, where a backwards step costs a redeploy and no data. Option 3 in the issue (a CI job resolving each digest to a version) buys a version comparison for those eight at the price of a network transport nothing else in CI needs; option 1's ledger costs a manual append per bump on sixteen automerged pins. Neither is worth it at that exposure. What this file makes executable is the FENCE rather than the acceptance: the expensive three may not quietly become auto-deployable while still carrying an unreadable pin. | `ansible/tests/deploy/test_channel_tag_digest_pins_stay_off_autodeploy.py:15` | 2026-09-10 |
| the discriminator is the PR id, never an empty `sha`. | `ansible/tests/deploy/test_landing_annotations.py:148` | 2026-09-06 |
| ` markers must reference each other, in both directions. | `ansible/tests/repo/test_adr_links.py:1` | 2026-08-24 |
| ` markers at the lines they govern, which `.claude/skills/homelab-review/ | `ansible/tests/repo/test_adr_links.py:4` | 2026-08-24 |
| ` | `ansible/tests/repo/test_adr_links.py:13` | 2026-08-24 |
| ") | `ansible/tests/repo/test_adr_links.py:34` | 2026-08-24 |
| ' marker. | `ansible/tests/repo/test_adr_links.py:258` | 2026-09-12 |
| alpha holds. | `ansible/tests/repo/test_adr_links.py:290` | 2026-09-12 |
| alpha and beta. | `ansible/tests/repo/test_adr_links.py:293` | 2026-09-12 |
| no repo-root prefix is required. matched 4 citations in the whole tree, because the docs overwhelmingly cite from context -- `roles/k8s/manifests/tasks/main.yml:112`, `deploy_logic.py:458`. A guard that checks four things is a guard that passes vacuously, which is why the count assertion below exists. The extension must begin with a letter, which is what keeps `127.0.0.1:3100` and `10.0.0.240:51820` from parsing as a file called `127.0.0.` with extension `1`. Four role docs and two networking docs cite host:port pairs exactly that way. | `ansible/tests/repo/test_documented_paths_exist.py:55` | 2026-08-27 |
| `git ls-files`, not an `rglob` plus a skip list. | `ansible/tests/repo/test_documented_paths_exist.py:71` | 2026-08-27 |
| minus `go` -- the only Go here is the traefik role's local plugin (files/cloudflare-realip/), while the docs cite UPSTREAM Go by basename (`docs/longhorn-backup-tiering.md`: Longhorn's `deltablock.go:117`, `s3.go:88`), which counting the plugin would turn into missing-file failures. | `ansible/tests/repo/test_documented_paths_exist.py:93` | 2026-09-24 |
| a suffix, never a bare basename. | `ansible/tests/repo/test_documented_paths_exist.py:107` | 2026-08-27 |
| docs/reference/decisions.md is excluded here, not from DOCS itself -- its own `path:line` citations (the "Decided" column) stay checked by the walk above, and only the | `ansible/tests/repo/test_documented_paths_exist.py:361` | 2026-09-03 |
| ` comment prose from across the tree rather than being authored here, so a node citation appearing in it is always a second copy of one the source file carries -- and the source is never itself in this corpus, since a role's `.yml`/`.j2` task file is neither a doc nor a runtime module. Checking the copy caught a citation the source has carried, unchecked, since before this page existed (`ansible/roles/k8s/netpol-baseline/tasks/main.yml` cites a test that moved to `test_k8s_autodeploy_batch_gates.py`), and separately turned an identifier that wraps across two comment lines in the source into two words once the generator joins them with a space, which the citation pattern below then reads as a shorter, nonexistent name. Neither is a bug in the mirror; both are properties of the source the generator is not in a position to fix. | `ansible/tests/repo/test_documented_paths_exist.py:363` | 2026-09-03 |
| a real socket, UDP. socket, but the exempt half's whole job is proving an exempt test reaches the real syscall; asserting the patch is off would assert the stub, not the effect. UDP makes that syscall a route lookup and nothing more — the TCP connect it replaced sent a SYN and waited on a timeout, and which errno came back depended on the host's routing. The exempt test asserts only that the failure, if any, is NOT the guard's: a host with no route to TEST-NET-1 raises ENETUNREACH, one with a default route returns at once. | `ansible/tests/repo/test_leakguard.py:84` | 2026-09-21 |
| host-shipped `ansible/roles/*/*/files/*.py` is EXEMPT, and the exemption is not cosmetic. Those programs run under the hosts' own interpreter, not the repo's uv env: `python3 -V` on daniel-server and daniel-pi both report 3.12.3 (measured 2026-09-05), and the k8s `files/*.py` run under whatever python their pod image ships. Below 3.14 there is no PEP 649, so annotations evaluate EAGERLY at def/class time and the import is load-bearing insurance against a forward reference — `def merge(self, other: Foo) -> Foo:` inside `class Foo` raises NameError without it. The repo suite cannot observe that: it runs on 3.14, where every such annotation is lazy either way. So the exemption is what keeps a green suite from authorising a cron-time NameError on a host. | `ansible/tests/repo/test_no_future_annotations_import.py:26` | 2026-09-05 |
| no path literal under the scripts tree belongs in this set, which is why the largest of those contexts is held by the floor below rather than by a named member. `lib/script_coverage.py` reads such a mention inside a test as a COVERAGE credit, and a census names modules without exercising them. A literal here therefore credits someone else's module to this guard: it took the infra_map facade's `model` off its importer's suite and failed `test_the_infra_map_facade_members_inherit_the_facades_suite`. The 200-module floor already carries that context, since it is most of the 200. | `ansible/tests/repo/test_no_future_annotations_import.py:45` | 2026-09-05 |
| `git ls-files`, not `rglob` — the same reason `_helpers.discover_docs` gives. | `ansible/tests/repo/test_testpaths_covers_every_test_file.py:50` | 2026-08-28 |
| no self-probe\n      # more\n      disable: true\n" | `ansible/tests/services/test_autoheal_does_not_watch_itself.py:79` | 2026-09-17 |
| ` marker in the authelia template rules out. | `ansible/tests/services/test_crowdsec_hub_install_stages_the_hub_tree.py:21` | 2026-09-05 |
| ` marker in services.yaml.j2, which accepts | `ansible/tests/services/test_headlamp_widget_mapping_order.py:15` | 2026-08-23 |
| the SMTP startup check stays OFF" in src | `ansible/tests/services/test_smtp_wiring.py:366` | 2026-09-09 |
| ` marker in the template. | `ansible/tests/services/test_wg_easy_peer_cluster_drop.py:14` | 2026-09-24 |
| marker " | `ansible/tests/setup/test_cron_scripts_publish_via_pr.py:430` | 2026-08-27 |
| relative_to(_REPO), not a suffix match -- the container contexts this skips are named by their in-repo path. | `ansible/tests/setup/test_host_python_invocations.py:42` | 2026-08-23 |
| strict, and deliberately constraining. | `ansible/tests/setup/test_k3s_stage_gates.py:62` | 2026-08-27 |
| `domain` is exempt. (ansible/secret_rotation.yml:108-110) because it is a hostname, not a credential — leaking it costs nothing, and every script that builds a URL names it. Left in, it flags 6 of the 9 candidate scripts plus one already converted, so the guard would refuse every group and be turned off rather than obeyed. Exempting the name is right; loosening the whole-word regex that finds it would not be. | `ansible/tests/setup/test_release_bin_groups_have_no_secrets.py:41` | 2026-08-29 |
| /api is deliberately NOT routed` marker in the traefik role's | `ansible/tests/staging/test_staging_expect_paths_match_a_route.py:21` | 2026-09-10 |
| Markdown stays out of ruff's scope. blocks inside .md, which pulled every prose file in the repo into a gate whose stated job is first-party Python — and its output mangles the plan docs, exploding readable two-element dict keys across four lines. Excluding *.md holds the gate to the same Python files ruff 0.15 covered; `uv run ruff format --check` prints the count, so don't restate one here. `ansible/collections` is the vendored third-party tree, never linted or formatted. | `pyproject.toml:190` | 2026-08-23 |
| the claude_worktree stand-in is out of scope too. a dotfiles module that the dotfiles repo lints at its own 3.10 floor, and a deployed-host test diffs the two. This repo's 3.14 target rewrites `except (A, B):` into PEP 758's unparenthesized form, which is a SyntaxError on 3.10 and breaks the byte match. | `pyproject.toml:197` | 2026-09-24 |
