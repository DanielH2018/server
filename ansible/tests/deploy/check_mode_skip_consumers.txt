# Consumers of a register check mode turns into a skip result, allowlisted by
# ansible/tests/deploy/test_conditional_register_consumers.py. They predate the #2315
# widening that found them; draining the list is #2323.
#
# Format: `<path under ansible/roles/>\t<task name>`, one entry per line, in `LC_ALL=C sort`
# order. Blank lines and `#` comments are ignored.
#
# SHRINKS ONLY. Each fix is a human judgement between `check_mode: false` + `changed_when:
# false` (a read of state that exists independently of the play) and `when: not
# ansible_check_mode` (a wait or a mutation), confirmed by a real `--check` of the tag from
# the host Ansible runs on. `test_the_allowlist_names_only_live_offenders` fails on an entry
# the rule no longer flags, so a fix cannot be applied and then quietly reverted, and a
# rename surfaces here instead of dropping a task out of the census.
k8s/claude-otel/tasks/dashboards.yml	Write the dashboard ConfigMap manifests
k8s/janitorr/tasks/verify.yml	Report the dry-run state
k8s/jellyfin/tasks/verify.yml	Report the VAAPI encode
k8s/jellyfin/tasks/verify.yml	Report the tone-mapping situation
k8s/media-volume/tasks/main.yml	Show the hardlink probe result
k8s/media-volume/tasks/sync.yml	Fail if the volume does not match the source byte for byte
k8s/media-volume/tasks/sync.yml	Refuse to mirror an empty source
setup/chezmoi_setup/tasks/main.yml	Fail with a usable message if GitHub authentication is missing
setup/claude_code/tasks/main.yml	Report that an interactive login is still required
setup/docker_install/tasks/install.yml	Add user to docker group
setup/fake_remux/tasks/main.yml	Assert Sonarr resolved to an address
setup/fake_remux/tasks/main.yml	Render the fake-remux config
setup/github_cli/tasks/main.yml	Report that an interactive GitHub login is still required
setup/hypervisor/tasks/guest.yml	Autostart the guest
setup/hypervisor/tasks/guest.yml	Define the guest
setup/hypervisor/tasks/guest.yml	Read the running guest's live interface
setup/hypervisor/tasks/guest.yml	Start the guest
setup/hypervisor/tasks/guest.yml	Stop a running guest whose live interface carries no egress fence
setup/hypervisor/tasks/install.yml	Stop libvirt's built-in default network
setup/hypervisor/tasks/install.yml	Stop libvirt's built-in default network from autostarting
setup/hypervisor/tasks/network.yml	Autostart the staging network
setup/hypervisor/tasks/network.yml	Define the staging egress fence
setup/hypervisor/tasks/network.yml	Define the staging network
setup/hypervisor/tasks/network.yml	Pin the staging network's UUID
setup/hypervisor/tasks/network.yml	Start the staging network
setup/initial_setup/tasks/system-tuning.yml	Keep the info-level forwarding out of /var/log/syslog
setup/k3s/tasks/agent_verify.yml	Fail if the node registered an address the host does not own
setup/k3s/tasks/server.yml	Fail if the bundled Traefik, servicelb or local-path provisioner is running
setup/sops_setup/tasks/main.yml	Seed ansible/.sops.yaml (first-host bootstrap only)
setup/sops_setup/tasks/main.yml	Show the Public Key to user
