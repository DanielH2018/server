{# Username comes solely from mqtt_username (single source of truth); strip any
   `user:` prefix baked into the stored hash so the two can never drift. The PBKDF2
   hash depends on the password + salt only, not the username, so re-prefixing is safe. #}
{{ mqtt_username }}:{{ mqtt_password_hash | regex_replace('^[^:]*:', '') }}
