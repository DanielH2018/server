- include_role:
    name: common
    tasks_from: setup_dirs.yml
  vars:
    dirs_to_create:
    - "{{ container_item.name }}/data/mods"
    - "{{ container_item.name }}/modpacks"


- include_role:
    name: common
    tasks_from: docker_deploy.yml
