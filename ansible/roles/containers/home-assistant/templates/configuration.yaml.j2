# Configure a default setup of Home Assistant (frontend, api, etc)
default_config:

# Entity friendly-name / icon overrides (templated — see customize.yaml.j2).
homeassistant:
  customize: !include customize.yaml

http:
  use_x_forwarded_for: true
  # Trust the whole Docker bridge range (RFC1918 172.16/12) so HA honors
  # X-Forwarded-For from Traefik regardless of which bridge subnet it connects
  # from — matches the trusted-proxy ranges in the authelia and traefik configs.
  trusted_proxies:
    - 172.16.0.0/12

# Root dashboard is YAML-mode: ui-lovelace.yaml IS the landing page, so the single-room
# setup opens straight to it with no sidebar click. This replaces the auto-generated
# storage-mode "Overview". Built-in cards only, so no `resources:` are needed.
lovelace:
  mode: yaml
