# Optional: This creates a simple web page at /all/ showing your available feeds
[listing]
enabled = true

# This defines your newly merged master calendar
[[calendar]]
slug = "master"
urls = [
    "example1.ics",
]

# Optional: You can create a second distinct merged feed if you want to keep some separate
# [[calendar]]
# slug = "work-only"
# urls = [
#     "https://calendar.google.com/calendar/ical/WORK/basic.ics",
# ]
