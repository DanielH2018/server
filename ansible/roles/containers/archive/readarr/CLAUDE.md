# readarr — Book/audiobook download manager (ARCHIVED)

**Not deployed.** Parked in `archive/`; see `../CLAUDE.md` for how to reactivate.

- **Image:** `lscr.io/linuxserver/readarr:develop`
- **Intended:** port 8787 · media net · Authelia: yes
- **Notable:** The *arr-stack app for books. On reactivation it depends on prowlarr
  (indexers) like sonarr/radarr — add a `meta/deps.yml`. Image is the `:develop` tag.
