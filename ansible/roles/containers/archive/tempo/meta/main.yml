---
galaxy_info:
  author: DanielH2018
  description: "Grafana Tempo — trace backend for Claude Code's OTLP spans"
  license: MIT
  min_ansible_version: "2.15"

# Tempo has no deploy-order dependency of its own — it is the SINK, and otel-collector is
# the producer that must come after it (see otel-collector/meta/deps.yml). Actual sequencing
# is computed by the toposort filter in deploy.yml reading meta/deps.yml, not by Ansible meta
# dependencies (which conflict with the include_role loop used in deploy.yml).
dependencies: []
