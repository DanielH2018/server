# calibre — E-book library server (ARCHIVED)

**Not deployed.** Parked in `archive/`; see `../CLAUDE.md` for how to reactivate.

- **Image:** `lscr.io/linuxserver/calibre:latest`
- **Intended:** port 8080 · media net · Authelia: yes
- **Notable:** Full Calibre desktop (via guacamole-style web access) for managing the
  e-book library; pairs with `calibre-web` (the lighter reader UI).
