# calibre-web — E-book reader web UI (ARCHIVED)

**Not deployed.** Parked in `archive/`; see `../CLAUDE.md` for how to reactivate.

- **Image:** `lscr.io/linuxserver/calibre-web:latest`
- **Intended:** port 8083 · media net · Authelia: yes
- **Notable:** Lightweight browse/read/OPDS front-end over an existing Calibre library
  (the `calibre` role manages that library).
