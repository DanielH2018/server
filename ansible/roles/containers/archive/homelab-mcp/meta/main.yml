---
galaxy_info:
  author: DanielH2018
  description: "Read-only MCP server exposing live homelab state to Claude Code"
  license: MIT
  min_ansible_version: "2.15"

# Logical ordering: requires traefik (the token-gated route lives in its file provider).
# Actual sequencing is computed by the toposort filter in deploy.yml reading meta/deps.yml,
# not by Ansible meta dependencies (which conflict with the include_role loop
# variable architecture used in deploy.yml).
dependencies: []
