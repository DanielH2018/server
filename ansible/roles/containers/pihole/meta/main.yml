---
galaxy_info:
  author: DanielH2018
  description: "Network-wide ad blocker with Unbound upstream DNS"
  license: MIT
  min_ansible_version: "2.15"

# Logical ordering: requires traefik; Unbound is co-deployed in the same Compose stack
# Actual sequencing is enforced by containers_list ordering in host_vars rather than
# via Ansible meta dependencies, which would conflict with the include_role loop
# variable architecture used in deploy.yml.
dependencies: []
