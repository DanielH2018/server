---
# Intentionally minimal. The cluster dashboard is where the full bookmark set lives; this
# one only needs the way back to the things that are still reachable when the house is not.
- Home:
    - Cluster dashboard:
        - abbr: HP
          href: https://homepage.{{ domain }}/
