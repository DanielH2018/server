---
galaxy_info:
  author: DanielH2018
  description: "Configarr — IaC custom-format/score sync for the Sonarr Anime profile"
  license: MIT
  min_ansible_version: "2.15"

# Logical ordering: requires the media network (traefik) and the Sonarr instance it syncs.
# Actual sequencing is computed by the toposort filter in deploy.yml reading meta/deps.yml,
# not by Ansible meta dependencies (which conflict with the include_role loop
# variable architecture used in deploy.yml).
dependencies: []
