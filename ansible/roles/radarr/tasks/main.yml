- name: Create radarr directories
  file:
    path: "/home/{{ SYS_USER }}/server/radarr/config"
    state: directory
    mode: 0755

- name: Create download directories
  file:
    path: "/home/{{ SYS_USER }}/server/media/downloads"
    state: directory
    mode: 0755

- name: Create movie directories
  file:
    path: "/home/{{ SYS_USER }}/server/media/movies"
    state: directory
    mode: 0755

- name: Create docker-compose from template
  template:
    src: docker-compose.yml.j2
    dest: "/home/{{ SYS_USER }}/server/radarr/docker-compose.yml"
    
- name: Deploy Radarr
  shell: docker-compose up -d --force-recreate
  args:
    chdir: "/home/{{ SYS_USER }}/server/radarr/"
  become: yes
  become_method: sudo