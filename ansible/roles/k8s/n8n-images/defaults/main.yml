---
# Auto-deploy stance — read by gitops_deploy (see the k8s auto-deploy coverage design).
k8s_autodeploy: true  # noqa var-naming[no-role-prefix]
k8s_autodeploy_reason: "builds two images via k8s/image-builder, which waits on each build for complete-or-failed and then asserts the new tag exists in the registry — a stronger gate than rollout status; note it renders no manifest of its own, so the shape guards pass over empty lists and the gate is image-builder's, asserted separately by test_gating_shared_roles_actually_wait"  # noqa var-naming[no-role-prefix]
