---
apiVersion: v1
kind: Service
metadata:
  name: healthchecks
  namespace: {{ k8s_namespace }}
spec:
  selector:
    app: healthchecks
  ports:
    - port: {{ container_item.port }}
      targetPort: {{ container_item.port }}
