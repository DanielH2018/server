---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ volume_claim_name }}
  namespace: {{ k8s_namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ volume_claim_storage_class }}
  resources:
    requests:
      storage: {{ volume_claim_size }}
