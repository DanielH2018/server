---
apiVersion: v1
kind: ConfigMap
metadata:
  name: qbittorrent-netns-reset
  namespace: {{ k8s_namespace }}
data:
  netns-reset.sh: |
{#
  Carried verbatim from files/ rather than templated: the script reads LAN_NETWORKS and PUID
  from the sidecar's own environment, so it has nothing to render, and a plain file is what the
  Pi-side image test in this role's CLAUDE.md runs unchanged.
#}
{{ lookup('file', playbook_dir + '/roles/k8s/qbittorrent/files/netns-reset.sh') | indent(4, true) }}
