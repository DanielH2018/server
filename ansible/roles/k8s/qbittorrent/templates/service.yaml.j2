---
apiVersion: v1
kind: Service
metadata:
  name: qbittorrent
  namespace: {{ k8s_namespace }}
spec:
  selector:
    app: qbittorrent
  ports:
    - name: webui
      port: {{ container_item.port }}
      targetPort: {{ qbittorrent_k8s_webui_port }}
