---
apiVersion: v1
kind: Service
metadata:
  name: ical-proxy
  namespace: {{ k8s_namespace }}
spec:
  selector:
    app: ical-proxy
  ports:
    - port: {{ container_item.port }}
      targetPort: {{ container_item.port }}
