---
# k8s/manifests creates this directory too, but this role deliberately runs BEFORE it — the
# workload must not start against a claim that does not exist — so it cannot rely on that.
# Both are idempotent.
- name: Create the manifest directory for {{ seed_volume_service }}
  tags: [config]
  ansible.builtin.file:
    path: "/etc/rancher/k3s/manifests/{{ seed_volume_service }}"
    state: directory
    mode: "0700"
    owner: root
    group: root
  become: true

- name: Render the volume claim for {{ seed_volume_service }}
  tags: [config]
  ansible.builtin.template:
    src: pvc.yaml.j2
    dest: "/etc/rancher/k3s/manifests/{{ seed_volume_service }}/pvc.yaml"
    mode: "0644"
    owner: root
    group: root
  become: true

# `changed_when` on 'created' rather than on rc: apply is idempotent and reports "unchanged"
# for a claim that already exists, which is the steady state on every deploy.
- name: Create the PVC for {{ seed_volume_claim }}
  tags: [deploy]
  ansible.builtin.command:
    cmd: "k3s kubectl apply -f /etc/rancher/k3s/manifests/{{ seed_volume_service }}/pvc.yaml"
  register: seed_volume_pvc
  become: true
  changed_when: "'created' in seed_volume_pvc.stdout"
