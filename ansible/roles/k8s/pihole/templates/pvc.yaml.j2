{% from 'pvc.yml.j2' import pvc with context %}
---
{% call pvc(pihole_k8s_claim, pihole_k8s_storage_class, pihole_k8s_size) %}
  # Not seeded from the Docker copy — blocklist state is declared in the role defaults and
  # reconciled by tasks/main.yml; everything else in /etc/pihole regenerates (see defaults).
{% endcall %}
---
{{ pvc(pihole_k8s_claim_2, pihole_k8s_storage_class, pihole_k8s_size) }}
