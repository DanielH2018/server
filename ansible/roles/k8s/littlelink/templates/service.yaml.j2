---
apiVersion: v1
kind: Service
metadata:
  name: littlelink
  namespace: {{ k8s_namespace }}
spec:
  selector:
    app: littlelink
  ports:
    - port: {{ container_item.port }}
      targetPort: {{ container_item.port }}
