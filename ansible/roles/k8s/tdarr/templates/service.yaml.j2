{% from 'service.yml.j2' import service with context %}
---
{% call service(container_item.name, container_item.port) %}
    # The web UI only. tdarr_k8s_server_port is deliberately absent — see the role defaults.
{% endcall -%}
