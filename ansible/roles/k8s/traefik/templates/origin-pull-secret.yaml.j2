{% from 'origin-pull.yml.j2' import origin_pull_ca_secret with context %}
---
# Cloudflare's origin-pull CA, for the `cloudflare-origin-pull` TLSOption in dynamic.yaml.j2.
# A Secret because that is the only shape Traefik's CRD provider reads a client CA from
# (`clientAuth.secretNames`); the content is public — see files/cloudflare-origin-pull-ca.crt.
{{ origin_pull_ca_secret(k8s_namespace) }}
