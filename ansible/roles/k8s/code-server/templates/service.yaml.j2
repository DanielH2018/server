---
apiVersion: v1
kind: Service
metadata:
  name: code-server
  namespace: {{ k8s_namespace }}
spec:
  selector:
    app: code-server
  ports:
    - port: {{ container_item.port }}
      targetPort: {{ container_item.port }}
