{% from 'origin-pull.yml.j2' import origin_pull_ca_secret with context %}
---
# The observability-namespace copy of Cloudflare's origin-pull CA: the `cloudflare-origin-pull`
# TLSOption in middlewares.yaml.j2 resolves its Secret here, not in the traefik role's namespace.
# Public content (roles/k8s/traefik/files/cloudflare-origin-pull-ca.crt); a Secret only because
# `clientAuth.secretNames` is the one shape the CRD provider reads a client CA from.
{{ origin_pull_ca_secret(k8s_observability_namespace) }}
