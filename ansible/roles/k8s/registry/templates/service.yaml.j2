---
apiVersion: v1
kind: Service
metadata:
  name: registry
  namespace: {{ k8s_namespace }}
spec:
  selector:
    app: registry
  ports:
    - port: {{ k8s_registry_port }}
      targetPort: {{ k8s_registry_port }}
