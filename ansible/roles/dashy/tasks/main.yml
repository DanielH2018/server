- include_tasks: directory.yml
  with_items:
    - "{{ role_name }}"

- name: Create local-conf.yml from template
  template:
    src: local-conf.yml.j2
    dest: "/home/{{ SYS_USER }}/server/dashy/local-conf.yml"

# - include_tasks: dns.yml
#   with_items:
#     - "{{ role_name }}"

- include_tasks: docker.yml
  vars:
    container: "{{ role_name }}"
