- include_tasks: directory.yml
  with_items:
    - "{{ role_name }}/config"
    - "media/movies"
    - "media/tvseries"

- include_tasks: dns.yml
  with_items:
    - "{{ role_name }}"

- include_tasks: docker.yml
  vars:
    container: "{{ role_name }}"
