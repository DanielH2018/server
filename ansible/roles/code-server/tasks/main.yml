- name: Creates directories
  file:
    path: "/home/{{ env.SYS_USER }}/server/code-server/config/workspace"
    state: directory
    mode: 0755

- name: Create Server Symlink
  file:
    src: /home/{{ env.SYS_USER }}/server/
    dest: /home/{{ env.SYS_USER }}/server/code-server/config/workspace/server
    state: link

- name: Create docker-compose from template
  template:
    src: docker-compose.yml.j2
    dest: "/home/{{ env.SYS_USER }}/server/code-server/docker-compose.yml"
    
- name: Deploy Code Server
  shell: docker-compose up -d --force-recreate
  args:
    chdir: "/home/{{ env.SYS_USER }}/server/code-server/"
  become: yes
  become_method: sudo
