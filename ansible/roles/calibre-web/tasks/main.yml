- include_tasks: directory.yml
  with_items:
    - "{{ role_name }}/config"
    - "data/media/books"

# - include_tasks: dns.yml
#   with_items:
#     - "{{ role_name }}"

- include_tasks: docker.yml
  vars:
    container: "{{ role_name }}"
