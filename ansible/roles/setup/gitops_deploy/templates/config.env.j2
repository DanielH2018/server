REPO_DIR=/home/{{ sys_user }}/server
BRANCH=master
HEALTH_TIMEOUT_S=300
# Wall-clock budget for the health-gate phase (see gitops_deploy.py RUN_BUDGET_S). Kept under the
# unit's TimeoutStartSec (25min) with ~5min of margin so the rollback redeploy still fits.
RUN_BUDGET_S=1200
DISCORD_WEBHOOK={{ gitops_deploy_discord_webhook }}
