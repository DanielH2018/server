# Kuma push credentials for setup-drift-check.sh — managed by Ansible (initial_setup role).
# 0640 root:{{ sys_user }}; sourced by the check, never inlined into it.
SETUP_DRIFT_PUSH_TOKEN={{ setup_drift_push_token }}
