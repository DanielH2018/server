---
- name: Update and upgrade system packages
  ansible.builtin.apt:
    update_cache: true
    upgrade: dist
  become: true

- name: Install required packages
  ansible.builtin.apt:
    name:
      - linux-raspi
      - openvswitch-switch-dpdk
      - wireguard
    state: present
  become: true
  when: inventory_hostname == 'daniel-pi'

- name: Pip Install
  ansible.builtin.pip:
    name:
      - ansible-lint
      - pre-commit

- name: Install required packages
  ansible.builtin.apt:
    name:
      - unzip
      - unattended-upgrades
      - update-notifier-common
    state: present
  become: true

- name: Set SSH directory permissions
  ansible.builtin.file:
    path: ~/.ssh
    mode: "0700"
    owner: ubuntu
    group: ubuntu
    recurse: true

- name: Configure SSH security settings
  ansible.builtin.lineinfile:
    path: "{{ ssh_config_path }}"
    regexp: "^{{ item.key }}"
    line: "{{ item.key }} {{ item.value }}"
  loop:
    - { key: "PasswordAuthentication", value: "no" }
    - { key: "PermitEmptyPasswords", value: "no" }
    - { key: "PermitRootLogin", value: "no" }
    - { key: "IgnoreRhosts", value: "yes" }
    - { key: "ChallengeResponseAuthentication", value: "no" }
  notify: Restart SSH

- name: Schedule sudo crontab for weekly restart
  ansible.builtin.cron:
    name: "Weekly system restart"
    minute: "30"
    hour: "7"
    weekday: "0"
    job: "/sbin/shutdown -r +5"
    user: root
  become: true

- name: Schedule Docker image cleanup
  ansible.builtin.cron:
    name: "Clean unused Docker images"
    minute: "30"
    hour: "6"
    job: "/usr/bin/docker image prune -a -f"
    user: "{{ ansible_user_id }}"
  become: true

- name: Schedule Docker Compose image pull
  ansible.builtin.cron:
    name: "Repull docker-compose"
    minute: "35"
    hour: "6"
    job: "/usr/bin/docker image pull ghcr.io/linuxserver/docker-compose:latest"
    user: "{{ ansible_user_id }}"
  become: true

- name: Create cron job to clear ansible.log weekly
  ansible.builtin.cron:
    name: "Clear ansible log file"
    minute: "0"
    hour: "6"
    weekday: "0" # 0 is Sunday
    job: "truncate -s 0 /home/{{ sys_user }}/server/ansible/ansible.log"
  become: true

- name: Install Git hooks
  ansible.builtin.command: pre-commit install
  args:
    chdir: "{{ playbook_dir }}"
    # Only run this if the hook doesn't exist yet
    creates: "{{ playbook_dir }}/.git/hooks/pre-commit"

- name: Enable IPv4 forwarding
  ansible.posix.sysctl:
    name: net.ipv4.ip_forward
    value: "1"
    state: present
    reload: true
  become: true

- name: Configure UFW firewall
  community.general.ufw:
    rule: allow
    port: ssh
    proto: tcp

- name: Allow WireGuard port 51820/udp
  community.general.ufw:
    rule: allow
    port: "51820"
    proto: udp
  become: true
  when: inventory_hostname == 'daniel-pi'

- name: Enable UFW firewall
  community.general.ufw:
    state: enabled
