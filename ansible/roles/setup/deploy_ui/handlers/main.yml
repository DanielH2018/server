---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart deploy-ui
  ansible.builtin.systemd:
    name: deploy-ui.service
    state: restarted
  become: true
