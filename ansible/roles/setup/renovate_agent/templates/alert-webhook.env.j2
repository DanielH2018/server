{{ ansible_managed | comment }}
# Read by renovate-agent-alert.service via EnvironmentFile=, and by nothing else. Separate
# from config.env for the reason the alert unit's header gives. Mirrors
# renovate_notify/templates/alert-webhook.env.j2.
ALERT_WEBHOOK={{ gitops_deploy_discord_webhook }}
