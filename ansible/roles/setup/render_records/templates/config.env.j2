# Render-record producer Kuma credentials — managed by Ansible (render_records role).
# 0640 root:{{ sys_user }}, read by the kuma-check-render-records unit's EnvironmentFile=.
KUMA_HOST=uptime-kuma.local.{{ domain }}
PUSH_TOKEN={{ render_records_push_token | default('') }}
