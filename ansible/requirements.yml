# Pinned to latest stable, validated against the runtime ansible-core (2.21.0 on
# Python 3.14, uv-tool managed). These were already the newest Galaxy releases as of
# 2026-06-07 (`ansible-galaxy collection install --upgrade` found nothing newer).
# Bump in lockstep with any ansible-core change.
collections:
  - name: ansible.posix
    version: 2.2.2

  - name: community.general
    version: 13.1.0

  - name: community.sops
    version: 2.4.0

  - name: community.docker
    version: 5.2.1
