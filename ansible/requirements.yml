# Pinned to latest stable, validated against the runtime ansible-core (2.21.x on
# Python 3.12, post 24.04 upgrade). Bump in lockstep with any ansible-core change.
collections:
  - name: ansible.posix
    version: 2.2.0

  - name: community.general
    version: 13.0.1

  - name: community.sops
    version: 2.3.0

  - name: community.docker
    version: 5.2.1
